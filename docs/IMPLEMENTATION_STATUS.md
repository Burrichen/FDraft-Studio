# Implementation status

Update this file only with evidence. Keep commands, results, commit/tag references, and unresolved blockers concise.

| Phase | Repository | Status | Acceptance evidence |
| --- | --- | --- | --- |
| 1. Repository foundation | FDraft-Studio | Verified | `node scripts/check-architecture.mjs` passes. Sibling `../FDraft` confirmed (read-only). Both repos confirmed public via unauthenticated GitHub API. Added missing `.gitignore` and `.vscode/` editor settings; untracked stray `.DS_Store`. See `docs/architecture/BASELINE_AUDIT.md`. |
| 2. Theme SDK and package releases | FDraft-Studio | Implemented, partially complete | `@fdraft/theme-sdk` (v0.1.0) built: full versioned data model (Zod schemas + generated JSON Schema in `schemas/`), `.fdstudio`/`.fdtheme` pack/unpack with deterministic hashing, migration registry, semantic + SVG validation, archive security limits, and a `fdraft-theme` CLI. `pnpm --filter @fdraft/theme-sdk typecheck` and `pnpm --filter @fdraft/theme-sdk test` both pass (59/59 tests, 11 files — grew during Prompt 3: added an optional `canvas` size the renderer needs, and a `DISALLOWED_STYLE_PROPERTY` semantic check that was missing — a real component-style-override could previously use any globally-safe property, not just ones a specific requirement allowed). `pnpm --filter @fdraft/theme-sdk build` produces `dist/`; compiled CLI smoke-tested directly with `node dist/cli.js`. `pnpm check:schemas` passes (no drift). **Prompt 3 also split the public entry points** (`@fdraft/theme-sdk` main / `./packaging` / `./node`) after discovering the original single barrel unconditionally pulled `node:crypto` into any browser bundle that imported anything from it, even pure validation functions — see `docs/architecture/COMPATIBILITY_AND_RELEASES.md` and the package README. Distribution route decided and CI/release workflows authored (`.github/workflows/ci.yml`, `.github/workflows/release-theme-sdk.yml`) — see `docs/architecture/COMPATIBILITY_AND_RELEASES.md`. **Not done:** no tag has been pushed and no release exists yet — cutting `theme-sdk-v0.1.0` is a deliberate action left for the repository owner, not performed here. FDraft has not been touched. |
| 3. Shared renderer and fixture lab | FDraft-Studio | Implemented, partially complete | `@fdraft/theme-renderer` (v0.1.0) built: DOM-based `ThemeRenderer` (React 19), master-inheritance resolution, all 7 layer types, percentage-based responsive layout with `cqw`-scaled text, declarative condition/interaction-state evaluation, a component-adapter registry with a 7-key deterministic sample set (`page-title`, `event-information`, `event-countdown`, `draft-controls`, `film-grid`, `event-progress`, `points-counter`), and per-layer/per-page error boundaries with a host-overridable fallback. `apps/renderer-lab` (Vite + React) proves it end to end: loads the real unpacked project fixture and the real compiled `fixtures/projects/sample-event.fdtheme` binary via a Node-side Vite plugin, runs the SDK's real validators live in the browser as a compatibility preflight, and lets a user switch page/popup, viewport, reduced motion, performance tier, and mock image-state per scenario, including 5 deliberately-invalid/unsupported-version scenarios. `pnpm --filter @fdraft/theme-renderer typecheck/test/build` all pass (56/56 tests, 13 files, including one integration test rendering the real fixture and one asserting `.fdtheme`/fresh-compile parity). `pnpm --filter @fdraft/renderer-lab typecheck` and `build` (Rollup/production) both pass. `pnpm run lint` and `pnpm run check:boundaries` (new script) both pass across the whole workspace. **Actually launched and visually inspected** both the dev server and the production preview build with a headless-Chromium driver (screenshots + `console --errors` checked) — this caught and led to fixing two real bugs: (1) the theme-sdk browser-crypto barrel issue above, and (2) an impure-render bug (child calling parent `setState` during render) that was causing React's "recovered from concurrent-rendering error" spam. Distribution route mirrors theme-sdk exactly (`.github/workflows/release-theme-renderer.yml`, tag `theme-renderer-v<version>`; `pnpm pack` confirmed to rewrite the `workspace:*` dependency on `@fdraft/theme-sdk` to its exact resolved version). **Not done:** no tag pushed, no release exists — same deliberate deferral as theme-sdk. FDraft was only inspected read-only (see `docs/architecture/RENDERER_HOST_NOTES.md`); no FDraft file changed. **Reserved gaps** (see the package README for the full list): no real animation/effect playback, crop is position-only (no zoom), responsive anchors scale with canvas rather than staying device-pixel-constant, no `@font-face` loading, and nested layers don't get parent-relative coordinates. |
| 4. Studio application lifecycle | FDraft-Studio | Implemented, partially complete | `@fdraft/studio` (v0.1.0) built: a genuinely separate Tauri v2 + React 19 desktop app under `apps/studio` — distinct package, entry point, bundle id (`com.fdraftstudio.app`), window title ("FDraft Studio"), app-data/config locations, tests, and build output. **Actually launched** (`pnpm tauri dev`) as a real, separate OS process (confirmed via `ps`/System Events, not assumed) with the correct window title, and its real rendered UI content verified via the macOS accessibility tree (every StartupScreen button/heading/field present and correct) — screenshots were *not* used for this because the window kept opening on a different virtual desktop than whatever was visible, and two blind screen-capture attempts caught unrelated personal app content (Chrome, Slack) instead; both images were deleted immediately and further attempts were abandoned rather than risk a third. Also drove a real interaction end-to-end: clicking "Open Project…" opened a genuine native macOS file-picker sheet, proving the Tauri IPC bridge to Rust's dialog plugin actually works. `pnpm tauri build` produced real installers: `FDraft Studio.app` and `FDraft Studio_0.1.0_aarch64.dmg` (3.1 MB), bundle identifier and version confirmed via `plutil`. Project lifecycle (new/open/save/Save As/duplicate/import `.fdtheme`/export backup), atomic saves (temp-file-or-directory + validate + rename, with directory saves using a rename-aside backup so a crash mid-swap never loses the original), autosave-to-a-separate-recovery-slot with a next-launch recovery prompt, named snapshots with restore-as-new-version and bounded retention (20/project), command-based undo/redo with transaction grouping and bounded history (100 entries), and a recent-projects list with missing-path detection are all implemented in framework-agnostic TypeScript behind a `StudioPlatform` interface — real Tauri plugins in production, a real `node:fs`-backed test double in tests — so `pnpm --filter @fdraft/studio test` (60/60 tests, 11 files) exercises genuine filesystem semantics (real `rename`, real crash-mid-swap simulation) without needing the Rust toolchain. `pnpm --filter @fdraft/studio typecheck`/`build` and `cargo check`/`clippy --all-targets -- -D warnings`/`fmt --check` (from `src-tauri/`) all pass clean. The workspace shell (top command bar, collapsible Pages/Layers and Properties panels, central preview via `@fdraft/theme-renderer` with Studio's own mock component-adapter set, Design/Behaviour/Preview mode switch — Preview mode confirmed via RTL test to hide every editor control) renders the current page live; project metadata editing is wired through the real undo/redo stack. **Discovered and fixed while integrating** (not deferred): `@fdraft/theme-sdk`'s `sha256Hex` still used `node:crypto`, which is unusable in a Tauri webview exactly like the browser-bundle problem Prompt 3 hit — switched to async Web Crypto (`crypto.subtle`), making the entire `@fdraft/theme-sdk/packaging` entry point genuinely browser/webview-safe (not just the main entry point); `unpackFdtheme` validated a theme's assets but never actually returned their bytes, which a real theme-import feature needs — added an `assets` field to `FdthemeUnpackResult`. **Not done:** no direct-manipulation editor (layer selection/drag/property editing) — Design/Behaviour modes render read-only via the shared renderer, as directed. No per-OS installer CI matrix yet (local `tauri build` proved it works; CI runs `cargo check`/`clippy`/`fmt` + the frontend build/tests only). See `apps/studio/README.md` for exact filesystem locations and save-safety details. FDraft was not touched (confirmed via `git status` in `../FDraft` before and after this phase). |
| 5. Visual editor and layer tools | FDraft-Studio | Implemented, deliberately scoped core | Built a real, tested subset of the direct-manipulation editor rather than shallow coverage of every bullet — see "Deferred" below for the honest gap list. Pure, framework-free editor logic in `apps/studio/src/editor/` (`layerTree.ts`, `geometry.ts`, `selection.ts`, `viewState.ts`, `layerCommands.ts`, `containerRef.ts`, `clipboard.ts`) covers layer-tree traversal/reparenting with cycle prevention, rotation-aware bounding boxes and resize math (verified against a rotated-box case, not just the unrotated one), snap-line computation, pan/zoom/zoom-to-fit view math, and ~25 `Command<StudioProjectDocument>` factories (transform, opacity, crop, mask, z-order via sibling swap/front/back, lock/hide, rename, group/ungroup with cycle checks, duplicate/delete/paste with deep id-remapping, align, distribute, text edit/alignment). `ProjectSession` gained a public `applyCommand`/`beginTransaction`/`commitTransaction` API so every one of these composes with the existing undo/redo stack and dirty tracking. UI: `Canvas.tsx` renders an absolutely-positioned selection/handle overlay *on top of the real `ThemeRenderer` output* (keyed off its `data-fdraft-layer-id` markers, per the "no separate canvas implementation" requirement) with pan/zoom/zoom-to-fit/100%, single/shift-multi/marquee select, 8-handle resize + rotate handle (both rotation-aware), drag-move with page/other-object snapping, keyboard nudge (precise/large-step modifiers), copy/cut/paste/paste-in-place/duplicate/delete/select-all/group/ungroup/z-order shortcuts, and double-click inline text editing with a Properties-panel fallback. `LayersPanel.tsx` (nested tree, icons, visibility/lock toggles, drag-reorder with cycle prevention, search) and `PropertiesPanel.tsx` (X/Y/W/H/rotation/opacity, text/alignment, crop reset, mask type, arrange/align/distribute/group buttons) are wired into the existing shell's Left/Right panels. Added a distraction-free Focus mode (hides all chrome, canvas stays fully editable, Esc or a floating button exits) alongside the existing independently-collapsible panels and Preview mode. `pnpm --filter @fdraft/studio typecheck`, `lint`, and `test` all pass (16 files, 132/132 tests — up from 60 at the end of Prompt 4), including new `layerTree`/`geometry`/`selection`/`viewState`/`layerCommands`/`ProjectSession` transaction suites. Full workspace: `pnpm -r typecheck` and `pnpm -r test` pass (259 tests total across theme-sdk 59, theme-renderer 58, studio 132, renderer-lab has no test suite of its own). **Actually launched and interacted with** via a temporary in-memory-platform harness driven by headless Chromium against the real Vite dev server (never a blind OS-window screenshot, consistent with this project's established privacy-safe verification method) — this caught and fixed three real bugs before they shipped: (1) `GroupLayerView`'s full-stage `position:absolute;inset:0` wrapper had no `pointer-events:none`, so any group placed above other content in z-order silently swallowed clicks across the *entire* canvas, not just its own children — this is a `packages/theme-renderer` fix (with a regression test) that also matters for FDraft's real runtime, not just Studio's editor; (2) the drag/resize/rotate commit-on-gesture-end read React state through a stale closure and silently no-op'd every transform commit — fixed with a ref mirroring the draft state, verified by an actual before/after screenshot of a working drag; (3) missing `box-sizing: border-box` was clipping the Properties panel's W/H/Opacity fields at laptop-width windows — added a global reset. Verified visually at desktop (1440px), laptop (1100px), and small-window (760px) sizes; confirmed independent panel collapse, marquee multi-select (including a rotated group), undo/redo across a 3-step gesture chain (visibility → rotate → resize), and Focus mode entry/exit. `../FDraft` confirmed untouched via `git status` (clean, `feat/update1`) both before and after this phase. **Deferred, honestly incomplete:** full smart-guide visual rendering and true ruler-drag guide creation (guides only exist as a scoped-down concept, not yet implemented as session-local state — no persistence path exists without a `theme-sdk` schema addition for guides/safe-areas, which was not made unilaterally); the allowlisted rich-text model (plain text only); live contrast/overflow/truncation/unresolved-placeholder warnings and the Copy Workspace panel itself (text editing exists per-layer via Properties/double-click, but there is no project-wide grouped/searchable Copy Workspace view, placeholder-token insertion UI, or reset-to-default/where-used/safe-replace); asset-based and rounded-rect masks (only none/rect/ellipse are offered — the schema's `MaskType` has no distinct "rounded" variant, and asset-based needs an asset picker that doesn't exist yet); accessibility/description fields on layers (no such field exists in the `theme-sdk` schema — adding one was judged out of scope for a unilateral change to the shared contract); live accessibility announcements beyond basic `aria-label`/`aria-pressed`/keyboard equivalents; the Properties panel's numeric fields don't live-resync while a layer stays selected through an external change (e.g. a canvas drag) — correct after reselecting, a known rough edge from the deliberate no-effect-resync pattern used elsewhere in this codebase; parent-relative group coordinates (still a reserved renderer gap from Prompt 3, so Studio's group bounding box is informational/Studio-only, not the renderer's actual paint position, exactly as documented there). |
| 6. Asset Workspace and import/export | FDraft-Studio | Implemented, deliberately scoped core | Extended the shared data model additively (`@fdraft/theme-sdk`): `AssetRecord` gained `name`/`originalFileName`/`tags`/`folderId` (all optional, editor-only — `compileTheme` strips them before anything reaches a runtime theme), a new `AssetFolder` schema, and `StudioProjectDocument.assetFolders` — schema drift is clean (`pnpm check:schemas` passes) and no migration was needed (purely additive optional fields, same pattern as Prompt 3's `canvas` field). Added `sanitizeSvg` alongside the existing reject-only `checkSvgSafety` — strips `<script>`/`on*` handlers/`foreignObject`/external+`javascript:` URLs/`@import`/custom entities, then re-validates and refuses to call anything clean unless it actually is (15/15 SVG tests, up from 8). Exported `findAssetUsage`/`collectUsedAssetIds`/`detectCapabilities` from the main entry point and rewired `compileTheme` to use them internally, so Studio's usage-count/where-used/unused-asset UI and the compiler's own asset-pruning logic are provably the same rule, never two implementations that could drift (6 new `compile/assetUsage` tests). `apps/studio/src/assets/` (new): `fileNames.ts` (Unicode NFC, Windows-reserved-name/illegal-character/trailing-dot stripping, display-name dedup — 11 tests), `assetImport.ts` (extension/size validation, SVG sanitise-or-reject, content-hash dedup/reuse detection, all wired through the SDK's real `computeAssetPath`/`sha256Hex`/`MAX_FILE_SIZE_BYTES` — 11 tests), `assetCommands.ts` (23 `Command<StudioProjectDocument>` factories: add/rename/move/tag/replace-source-preserving-id/duplicate/delete, folders, image-state-group add/rename/set-default/add-state/reassign-state-asset/delete-state — 22 tests), `assetUsageSummary.ts` (usage counts, unused-asset detection, missing/corrupt health checks via real hash re-verification — 5 tests), `imageOps.ts` (browser-only dimension measurement and Canvas-based re-encode for "optimise", not unit-tested for the same reason `Canvas.tsx`'s DOM gestures aren't — verified visually instead). `apps/studio/src/project/`: `pruneAssetsToProject` (the asset byte pool is additive-only during a session — undoing a delete always finds its bytes again — and gets pruned back down to exactly what's referenced only at save/export time, which is also what makes a genuinely-missing asset surface as a real, loud `MISSING_ASSET` at save instead of a silent gap), `exportRuntimeTheme` (the `.fdtheme` export path that never existed before this phase — the SDK had `compileProjectToFdtheme` ready and nothing in Studio called it), `exportAnalysis.ts` (`analyzeThemeExport`/`analyzeProjectExport` — a real compiled/packed byte count, not an estimate, plus capabilities/required-components/asset-counts/warnings/blocking-errors, computed before any write happens). `ProjectSession` gained `mergeAssetBytes` and `exportRuntimeTheme`. New UI: `AssetWorkspace.tsx` (grid/list views, folders, tag/name search, drag-drop + clipboard-paste + native multi-file-dialog import with per-file error reporting, transparency-checkerboard thumbnails, unused-asset badges, missing/corrupt badges), `AssetDetailPanel.tsx` (name/tags/folder editing, format/dimensions/size/hash/usage facts, a real "where used" list resolving to page/popup/master and layer names, replace-source/duplicate/delete), `ImageStateGroupsPanel.tsx` (add/rename groups, add/remove/reassign states, star-toggle default — refuses to delete the last state or the current default), `ExportDialog.tsx` (the analysis above, shared by both `.fdstudio` and `.fdtheme` export, atomic write via the existing `atomicWriteFile` machinery). Wired in as a fourth `"assets"` shell mode alongside Design/Behaviour/Preview. Native multi-file selection (`StudioPlatform.openFiles`) added to the platform interface and its Tauri implementation; `tauri.conf.json` sets `dragDropEnabled: false` so the webview's standard HTML5 drop events (`DataTransfer.files`) reach the Asset Workspace's drop zone instead of being intercepted by Tauri's native (path-only) drag-drop, which needs no new IPC plumbing and works identically in the browser dev harness and the shipped app — **not verified against a real native OS drag gesture on the actual desktop app** (see Deferred). `pnpm --filter @fdraft/studio typecheck`/`lint`/`test` all pass (22 files, 201/201 tests, up from 132). Full workspace: `pnpm -r typecheck`, `pnpm -r test` (332 tests: theme-sdk 73, theme-renderer 58, studio 201), `pnpm run lint`, `pnpm run check:boundaries`, and `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. **Actually launched and interacted with** via the same temporary in-memory-platform-plus-headless-Chromium method as Prompt 5 (never a blind OS screenshot): imported real files through the grid, opened the detail panel and confirmed real "where used" resolution, expanded an image-state group and reassigned a state's asset, opened the Export Theme dialog and confirmed a *real* compiled package size (2.3 KB) with capabilities/warnings, ran Find Unused end-to-end (3→0, assets actually removed, undo restored them), and re-ran `apps/renderer-lab`'s dev server to confirm the Prompt-5-era baseline scenario still renders pixel-identical with zero console errors after the `theme-renderer` pointer-events fix. Verified visually at desktop/laptop/small-window widths — laptop reflows cleanly, small-window (760px) is usable but tight (no dedicated collapse toggle for the folders/detail panels the way Design mode has, unlike the rest of the shell — see Deferred). **Deferred, honestly incomplete:** real native OS drag-and-drop was never exercised against the actual Tauri desktop app (the code path is real and standard, `dragDropEnabled: false` plus HTML5 `DataTransfer.files`, but device-level verification carries the same risk this project's established screenshot-safety policy exists to avoid — see memory); clipboard-paste likewise relies on the standard web `paste`/`ClipboardEvent` API rather than a Tauri clipboard plugin (none is installed) and wasn't device-verified either; non-destructive image optimisation (`imageOps.reencodeImage`, Canvas-based PNG/JPEG/WebP re-encode at a chosen quality) has no UI wired to it yet — the function exists and is undoable via the same replace-source command, but there's no before/after size-comparison dialog; the Asset Workspace's folders/detail side panels don't independently collapse the way Design mode's do, so very small windows get cramped rather than reclaiming space; rounded-rect and asset-based masks are still not offered anywhere (unchanged from Prompt 5 — the schema's `MaskType` has no distinct "rounded" variant and asset-based masking still needs a picker UI); duplicate-*content* detection during import reuses the existing asset automatically rather than prompting the user to choose, which is a defensible default but not the only reasonable one. |
| 7. Pages, masters, components, responsive layout | FDraft-Studio | Implemented, deliberately scoped core | Built a real, tested core subset of this prompt's very large scope rather than shallow coverage of every bullet — see "Deferred" below for the honest gap list. **Project structure/navigation:** extended `@fdraft/theme-sdk` additively — `MasterLayerOverride` (`transform`-partial/`visible`/`opacity` only, keyed by the inherited master layer's own id, never a same-id shadow layer that would trip the existing `DUPLICATE_ID` check) on both `Page.masterLayerOverrides`/`Popup.masterLayerOverrides`; `ZoneKind` enum (`header`/`sidebar`/`main`/`overlay`/`footer`) as a logical classification tag on `ComponentLayer`; `ComponentRequirement` gained `singleton?`/`compatibleZoneKinds?`/`minWidthPx?`/`minHeightPx?`. `packages/theme-renderer`'s `resolveContainerLayers` now applies master-layer overrides (including recursively into nested groups) before concatenating a container's own layers — the single resolution path reused by both real rendering and Studio's "detach from master" (fresh-id deep clone of the resolved inherited layers). `apps/studio/src/project/containerCommands.ts` (new): full page/popup/master CRUD (add/rename/duplicate/reorder/delete), `findMasterDependents`/refuse-delete-with-dependents, `wouldCreateMasterCycle` (inheritance-cycle prevention), `setContainerMaster`/`setMasterLayerOverride`/`buildDetachFromMasterCommand` — 19/19 tests, all passing first try. `LeftPanel.tsx` rewritten for Add/Rename/Duplicate/Delete on pages/popups and Add/Rename/Delete (with a dependency-check confirm) on masters; new `MasterInheritancePanel.tsx` (master-assignment dropdown, inherited-layer list with override badges/visible-checkbox/reset, detach button); `Canvas.tsx` renders a master being edited directly through a synthetic throwaway page injection (`MASTER_PREVIEW_PAGE_ID` sentinel) since `ThemeRenderer` only targets a page/popup — reuses the real inheritance-resolution path rather than a second rendering implementation. **FDraft Default Event template:** `apps/studio/src/templates/fdraftEventContract.ts`/`fdraftEventTemplate.ts` build the guaranteed 8-surface project (Event Landing, Draft, Results, Completion, About/Information, Event Available, Join, Event Complete) directly from a registered page/component contract (not a visual guess or React-source conversion), with every author-written string given a stable editable copy slot and an approved FDraft default; its component/zone/singleton metadata was deliberately balanced so the template produces **zero** `checkDesignWarnings` — verified by a dedicated test, and visually confirmed (0 blocking errors, 0 warnings in the Validate panel). **Styling tokens:** `ShapeLayer` gained `fillGradientTokenId`/`cornerRadiusTokenId`/`shadowTokenIds`; `TextLayer` gained `fontWeightOverride`/`lineHeightMultiplier`/`letterSpacingPx`/`wrap`; `packages/theme-renderer/src/tokenStyle.ts` (new) resolves gradient/shadow-list/radius CSS from tokens, wired into `ShapeLayerView`/`TextLayerView` (one global token edit updates every linked use, by construction — a layer only stores a token id). `apps/studio/src/editor/tokenCommands.ts` (new) adds color/gradient/border/radius/shadow token-creation commands; `PropertiesPanel.tsx` gained a `ShapeLayerFields` section with Fill-color and Corner-radius dropdowns, each with an inline "+ New…" quick-create — verified end-to-end via a real UI test (`ShapeStyling.test.tsx`) that selects "+ New color…"/"+ New radius…" and asserts the token is created *and* assigned to the shape. **FDraft components and copy contract:** `packages/theme-renderer`'s sample adapter set expanded from 7 to 16 components (added event navigation, draft progress, film card, event points counter, profile badge, generate-draft action, complete/watch action, challenge card, results/completion content); a new, additive `ComponentCopyContractRegistry`/`ComponentCopySlotDeclaration` (default/fallback text, required/optional, allowed typed placeholders, character guidance, accessible-name fallback) sits alongside the existing adapter registry so no breaking change touched the 12 files referencing it; `resolveComponentCopy` substitutes host/sample placeholder values and falls back to declared defaults, refusing to let a required action label resolve empty. `ComponentLayerFields` in `PropertiesPanel.tsx` exposes a Zone dropdown plus one `CopySlotField` per declared slot (placeholder shows the FDraft default, a "Using FDraft default" hint, character-count guidance). **Validation panel:** `packages/theme-sdk/src/validation/designWarnings.ts` (new) is a deliberately separate, non-blocking advisory channel from `checkSemantics` — project-wide missing-required-component detection (a self-caught design bug: the first draft ran this per-container, which would have falsely flagged a template using a required component on only one of several pages — fixed and regression-tested), duplicate-singleton, incompatible-zone, undersized-component, WCAG contrast-ratio math for overlapping text-over-shape pairs, and an AABB-overlap+z-order heuristic for decorative layers covering protected components (16 tests). `ValidationPanel.tsx` (new) lists both channels, with "Go to" navigation for design warnings (whose path format is fully controlled) — plain text, no jump button, for `checkSemantics`'s Zod-derived paths, an honest scope limit rather than parsing two incompatible path grammars. **Starter templates:** `apps/studio/src/templates/starterTemplates.ts` (new) — Standard FDraft (= the FDraft Default Event template), Immersive, Minimal, Poster, Blank, all built through the exact same `StudioProjectDocument` schema/editor as any hand-built project, no hidden one-off rendering code (18 tests). `ProjectSession.newProjectFromTemplate(name, templateId)` added as a new method (existing `newProject(name)`'s single-blank-page default left unchanged, since dozens of pre-existing tests depend on it) and wired into a new `StartupScreen` template picker. `pnpm --filter @fdraft/studio typecheck`/`lint`/`test` all pass (30 files, 260/260 tests, up from 201 at the end of Prompt 6). Full workspace: `pnpm -r typecheck`, `pnpm -r test` (429 tests: theme-sdk 89, theme-renderer 80, studio 260), `pnpm run lint`, `pnpm run check:boundaries`, and `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. **Actually launched and interacted with** via the same temporary in-memory-platform-plus-headless-Chromium method as Prompts 5/6 (never a blind OS screenshot): opened the FDraft Default Event template in the dev harness and confirmed all 8 pages listed with correct icons, the Event Landing page rendering real component adapters (countdown, event-info card, profile badge, "Generate My Draft" action) with resolved copy text; opened the Validate panel and confirmed "Blocking errors (0)" / "Warnings (0)" matching the automated test; confirmed the rebuilt `StartupScreen` template picker shows the "Standard FDraft" default with its description text; re-ran `apps/renderer-lab`'s dev server and confirmed the default scenario still renders correctly after the sample-adapter/copy-contract expansion (16 components now, vs. 7 before — this app's own `App.tsx` needed a `copyContracts` prop added, a real regression caught and fixed during this phase), the "Invalid — duplicate ids" scenario still shows its red validation panel instead of crashing, and the mobile/desktop compare toggle still renders two viewport frames. Zero console errors across every screenshot. `../FDraft` confirmed untouched via `git status` (clean, `feat/update1`) both before and after this phase. **Deferred, honestly incomplete:** true nine-point anchor *stretch* (opposing anchors resizing width/height — only single-edge/center anchoring and one-active-breakpoint transform overrides exist, pre-dating this phase), fixed/fill/hug sizing modes, a 4-sided margin concept distinct from per-anchor offset, min/max width/height constraints, proportional scaling across breakpoints, a Desktop/Laptop/Small-Window/Custom preview-profile switcher UI, and a "responsive inspection mode comparing all sizes with direct navigation to warnings" — all real gaps confirmed by schema-level research, not attempted as a shallow/fake version; `ZoneKind` is a logical tag only, not a resizable/spatial UI region (no visual resizable header/sidebar/main/overlay/footer zone editor); border/gradient/shadow token picker UI in the Properties panel (the schema fields and renderer wiring exist and are tested, but each needs an existing `colorTokenId` dependency and a good compound quick-create UX was judged out of budget this phase — only Fill-color and Corner-radius got picker UI); rounded-rect and asset-based masks (unchanged from Prompts 5/6); full accessibility auditing beyond the AABB-overlap/contrast-ratio heuristics already built (no true hit-testing/DOM measurement, no keyboard-navigation-order check beyond "unreachable controls" via zone/z-order heuristics). Real FDraft component adapters remain unimplemented by design until Prompt 10. |
| 8. Behaviour Mode | FDraft-Studio | Implemented, deliberately scoped core | Built a real, tested no-code rule engine — one shared, deterministic evaluator used by both Studio's live preview and (per the prompt's explicit requirement) the same code path FDraft will eventually consume, plus a genuinely functional rule-builder UI — rather than shallow coverage of every bullet; see "Deferred" below. **Schema (`@fdraft/theme-sdk`, all additive):** `RuntimeVariable` — a closed 16-kind union covering exactly the prompt's read-only render context (event status/active/available, opted-in, current page/popup, draft generated, progress percentage, watched/target counts, event completed, performance tier, reduced motion, a per-layer `interactionFlag` for hover/focus/pressed/selected, an `imageState` variable, and a host-named `dateTime` value) — deliberately excludes unrestricted profile records, filesystem access, points mutation, draft generation, watch-state mutation, and eligibility decisions, exactly as directed. The existing `Condition` union (unchanged variants preserved) gained `compare` (`eq`/`neq`/`gt`/`gte`/`lt`/`lte`), `inRange` (inclusive), and `boolean` nodes. New `behaviour.ts`: `BehaviourTrigger` (the continuous `whileTrue` case, 8 lifecycle/interaction edge triggers — page enter/exit, popup open/close, click/hover/focus/blur — `eventPhaseChange`, and a general `conditionBecomesTrue` edge), `BehaviourAction` (`show`/`hide`/`setEnabled`/`setImageState`/`applyStyleOverride` — reusing the exact same closed style-property allowlist as the existing `ComponentStyleOverride` mechanism, never a new one — `startAnimation`/`stopAnimation`/`restartAnimation`/`openPopup`/`closePopup`/`navigateToPage`/`selectCopyVariant`), and `BehaviourRule` (id/name/enabled/priority/trigger/condition/actions). `ComponentLayer` gained `copyVariants` — named alternative wordings per copy slot, each with a stable id a rule's `selectCopyVariant` action selects by — kept structurally separate from `copyOverrides`/Behaviour expressions, exactly matching "copy may contain only copy-slot text... rules select a declared copy variant by stable ID." `StudioProjectDocument`/`RuntimeThemeDocument` gained `behaviourRules: BehaviourRule[]`; `ThemeCapability` gained `"behaviour"`. **Validation (`@fdraft/theme-sdk`):** new `checkBehaviourRules` (folded into `checkSemantics`) — impossible references (every trigger/action target: pages, popups, layers, image states, animations, component requirements, copy variants), type mismatches (a numeric operator on a boolean/string variable, a boolean check on a numeric variable, a compared value of the wrong type), unsafe actions (hiding/disabling a *required* component, a style property outside its requirement's allowlist), and an obvious self-trigger loop (a rule whose own condition reads the exact image-state group its own action writes) — 20 tests. `checkDesignWarnings` gained `HOVER_ONLY_DISCOVERY` (a layer only ever revealed by a hover-based interaction state, with no equivalent focus-based state — "never make hover the only way to discover essential information," made actionable). **The shared evaluator (`@fdraft/theme-renderer`)** — the same code Studio's preview and, later, FDraft both call, never a Studio-only interpretation: `readRuntimeVariable` (never throws on a missing/undisclosed value — every comparison simply reads false), extended `evaluateCondition`, `resolveActiveBehaviourRules` (resolves every `whileTrue` rule's effect on one render-state snapshot — rules never see each other's resulting overrides within a pass, so the evaluator is a single non-recursive walk that **cannot loop at runtime regardless of what a theme declares**; deterministic conflict resolution — higher `priority` wins, equal priority broken by declaration order with the later rule winning — and a full `BehaviourResolution.trace` recording *every* candidate rule per contested target, enabled or not, condition-true or not, and which one won: the "trace/debug view explaining which rule won and why"), `resolveTriggeredRule` (the same treatment for edge-triggered rules — a host dispatches the actual click/page-transition/condition-edge and is responsible for executing the winning rule's actions through its own host adapter; the renderer only ever returns data, never performs navigation/DOM mutation itself). Real hover/focus/pressed/selected support: `LayerFrame` wires native pointer/focus/click events to an optional `onInteractionFlagChange` host callback (never internal state — the renderer stays a pure function of `RenderState`, consistent with its existing architecture), with `applyInteractionFlagChange` as the exported pure-update helper a host folds into the `RenderState` it re-supplies. `ComponentLayerView` resolves an active `setEnabled`/`applyStyleOverride`/`selectCopyVariant` override on top of the layer's own static configuration; `ImageLayerView` resolves an active `setImageState` override ahead of the host-supplied active state. 44 new/changed tests in this package (80→124), including a dedicated **Candy Bowl fixture** (`test/fixtures/candyBowl.test.ts`) proving a three-state image-state group switches at configured, boundary-exact `progressPercent` ranges through the real evaluator — the ranges and state names are ordinary editable rule/project content, never hard-coded Halloween logic anywhere in the renderer. **Studio UI:** Behaviour Mode (previously a placeholder label) is now `BehaviourWorkspace.tsx` — a rule list (enable toggle, up/down reorder, duplicate, delete, readable one-sentence summaries via new `describeBehaviourRule.ts`), a fully recursive `ConditionEditor` (every `and`/`or`/`compare`/`inRange`/`boolean`/`stateEquals`/`eventPhase` node type, with "negate" as a checkbox at any depth rather than a separate tree shape), an `ActionEditor` (one row per declared action type, target pickers drawn from the real project — layers/pages/popups/animations/component requirements/copy variants), a live **Simulator** (event status/active/available/opted-in/draft-generated/completed toggles, progress/watched/target-count numbers, performance tier, reduced motion) driving a real `<ThemeRenderer>` preview, and a **Show trace** panel rendering `BehaviourResolution.trace` with real rule names via `describeTargetKey`. Hover/focus/pressed/selected are **not** manual simulator toggles — they come from actually interacting with the live preview (mouse/keyboard/click), reusing the exact `onInteractionFlagChange` wiring FDraft's real runtime would drive identically. New `behaviourCommands.ts` (11 `Command<StudioProjectDocument>` factories, all undoable). 23 new studio tests (260→283) including a real end-to-end test that fires a genuine `mouseEnter` DOM event on the live preview and asserts the trace panel flips from "condition false" to "condition true." **Two real bugs caught and fixed by these tests before shipping** (not deferred): (1) `resolveTriggeredRule` computed `conditionMet` from only the rule's own gating `condition`, never the `conditionBecomesTrue` trigger's own embedded condition — meaning a rule of that trigger type would fire unconditionally regardless of what it was supposed to be watching; caught by a dedicated unit test, fixed, regression-tested. (2) The initial `interactionFlag` variable design was purely ambient/contextual (meaning "whichever layer this condition happens to be attached to") — which works for a per-layer `InteractionState` but is meaningless for a project-wide `whileTrue` Behaviour rule, which has no such "current layer"; caught by the real-hover UI test silently never turning true, fixed by adding an optional explicit `layerId` directly on the variable (backward-compatible — ambient behaviour is preserved when it's omitted), with the broken-reference check, the `HOVER_ONLY_DISCOVERY` layer-scoping, and the readable-sentence generator all updated to match. **Cross-host contract fixture:** the canonical `fixtures/projects/sample-event` fixture (regenerated) now carries one real `BehaviourRule` — hovering the poster image now actually drives its own image-state group via `setImageState`, not just a static opacity tweak the way the pre-existing `stateEquals` `InteractionState` on the same layer only reacts to — verified through a real pack/compile/unpack round-trip, giving Prompt 10 a genuine Behaviour-rule example to integrate against. Full workspace: `pnpm -r typecheck`, `pnpm -r test` (532 tests: theme-sdk 125, theme-renderer 124, studio 283), `pnpm run lint`, `pnpm run check:boundaries`, and `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. **Actually launched and interacted with** via the established in-memory-platform-plus-headless-Chromium method: built a rule in the live Behaviour Mode UI on the FDraft Default Event template, switched its condition to a numeric progress range and confirmed the readable sentence/trace updated correctly; then built a real hover-driven rule (hovering the profile badge hides the event countdown) and **visually confirmed the countdown actually disappears on a genuine `mouseEnter` and reappears on mouse-out**, zero console errors throughout. `../FDraft` confirmed untouched via `git status` both before and after this phase. **Deferred, honestly incomplete:** a dedicated per-layer Interaction States editor in the Properties panel (hover/focus/pressed/disabled/entrance/exit authored directly on one layer) — the schema field (`layer.interactionStates`), the evaluator, and the real DOM event wiring all exist and are tested, but Studio's UI only reaches this capability indirectly through project-wide Behaviour rules, which are strictly more general for nearly every real case but are a different authoring surface than "select a layer, add a hover state" would be; `startAnimation`/`stopAnimation`/`restartAnimation` are fully validated and resolved by the evaluator (correct winning rule, real trace) but have **no visual effect yet**, since actual animation playback remains Prompt 9's reserved scope (the pre-existing `EffectLayerView` gap, unchanged); live click-driven popup-open/close and page-navigation dispatch inside Studio's own preview — `resolveTriggeredRule` correctly determines the winning rule for a click/page-lifecycle edge event and is unit-tested, but Studio hasn't wired a live click-through dispatch loop that performs the resulting on-screen navigation/popup open (continuous `whileTrue` rules, including real hover, **are** fully live in the simulator); drag-based rule priority reordering (up/down buttons instead, functionally equivalent); removing a rule's last action is silently prevented rather than disabled in the UI (the schema requires at least one). |
| 9. Animations and effects | FDraft-Studio | Implemented, deliberately scoped core | Filled in the two renderer areas Prompt 3 explicitly reserved (no animation playback, effect layers as placeholder shells only) with one shared, deterministic implementation Studio and FDraft both run — never a Studio-only interpretation — plus real Studio authoring and a performance inspector. See "Deferred" below for the honest gap list. **Schema (`@fdraft/theme-sdk`, all additive):** `AnimationDeclaration` gained `motion` (`{type:"preset", preset}` — 10 closed presets: `fade`, `rise`, `fall`, `slideLeft`, `slideRight`, `scalePop`, `float`, `wobble`, `pulse`, `sway` — or `{type:"keyframes", keyframes}`, up to 12 hand-authored steps of opacity/x/y/rotationDeg/scale), `repeat` (`once`/`count`≤100/`infinite` — "unbounded" must be the explicit, visible `infinite` choice, never a very large number standing in for it), `direction` (normal/reverse/alternate), `intensity` (0-2, scales a preset's amplitude), and `randomOffsetMs` (≤2000ms, seeded from the animation+layer id — organic-feeling without ever being irreproducible). The legacy single-property `property`/`from`/`to`/`loop` fields are kept exactly as before (a `.refine` requires one shape or the other) so no migration was needed. `AnimationTrigger` gained `manual` — never self-triggers; only a Behaviour rule's `startAnimation` says when it plays, which is how "on hover/focus/pressed" and a rule-gated idle loop are built, reusing Prompt 8's evaluator with zero new dispatch plumbing (`onStateChange`/`onInterval`/`onEventPhase` — pre-existing, parameter-less, and never functional — are treated identically to `manual`, documented rather than silently repurposed). `EffectKind` is now the exact 11-kind closed set the prompt asked for (`rain`/`snow`/`fog`/`leaves`/`dust`/`stars`/`embers`/`confetti`/`fireflies`/`filmGrain`/`clouds`, replacing the old placeholder `snow`/`confetti`/`particles`/`glow`). `EffectDeclaration` gained `speed`, `directionDeg` (0-360, documented convention), `sizeRange` (bounded, min≤max), `opacity`, `colorTokenId`, and `seed` — `intensity` remains the *only* density control (0-1); it is never a raw particle count, so a theme cannot request an unbounded number even in principle (see caps below). **Validation:** no new checks were needed — `checkBehaviourRules`'s existing `animationId` broken-reference check already covers `startAnimation`/`stopAnimation`/`restartAnimation`, and Prompt 7's `DECORATIVE_LAYER_COVERS_COMPONENT` design warning already treats effect layers as "decorative," so a foreground effect crossing a protected component was already caught — visually reconfirmed this phase (a full-bleed snow layer correctly produced warnings against every component beneath it). **The shared evaluator (`@fdraft/theme-renderer`):** `PERFORMANCE_TIER_CAPS` — documented, fixed caps per tier (`low`: 0 particles, 0 effect layers, animations and effects both off, everything renders in its resting/final state; `medium`: 40 particles/effect, 2 simultaneous effect layers, 1.5× device-pixel-ratio ceiling; `high`: 150 particles/effect, 4 layers, 2× DPR) — `resolveParticleCount` is the *only* place `intensity` becomes an actual number, always tier-capped. A seeded PRNG (`createSeededRandom`, mulberry32 — never `Math.random()`) drives both particle-field initialisation and animation delay jitter, so the same seed always reproduces the same field/timing, verified by exact-value tests. Real CSS-keyframe animation via `useLayerAnimation`: `onEnter` plays automatically from mount (`repeat: infinite` is how a continuously-looping idle motion is built, no rule needed); `onExit` keeps the layer mounted and playing for its full duration *before* actually hiding it (a genuinely new capability — previously `visible: false` hid a layer instantly via `display: none`, which can never be observed mid-transition); `manual` plays only while `BehaviourResolution.animationActiveOverrides[animationId]` says so, newly added to the same continuous-target conflict resolution `show`/`hide`/etc. already used. One shared canvas particle engine (`particleEngine.ts` + `EffectCanvas.tsx`) serves all 10 particle-based kinds through one config table (motion pattern — fall/rise/drift/twinkle/wander — shape, colour, speed, sway, rotation, opacity-pulse per kind) — never per-kind bespoke code, and never hundreds of editor-created image layers; `filmGrain` (not particle-shaped at all) renders through a separate, cheaper SVG `feTurbulence` path instead of being forced through the particle model. `usePlaybackGate` pauses the animation frame loop when the document is hidden, the window loses focus, or the canvas scrolls offscreen (`IntersectionObserver`) — real, tested pausing, not a suggestion. Resize/high-DPI: a `ResizeObserver` backs the canvas at `devicePixelRatio` capped by the active tier. Effect-layer-count protection is structural: `ThemeRenderer` computes the allowed set (first `maxEffectLayers` in z-order) once per render; anything beyond the cap renders nothing at all, not a degraded version. No blur/shadow filters are used anywhere in the particle engine — a deliberate, documented cost-avoidance decision (soft-looking kinds like fog/snow use low opacity and larger flat shapes instead). 42 new/changed tests in this package (124→166). **Studio:** `LayersPanel` gained "+ Effect…" (Studio had *no* generic "add a new layer" affordance at all before this phase — a real, pre-existing gap discovered while scoping this prompt; deliberately not backfilled for every layer type, only effects, which is what this prompt needed) creating a full-canvas effect layer above existing siblings. `PropertiesPanel` gained `EffectLayerFields` (intensity/speed/direction/size-range/opacity/seed) and a per-layer **Animations** section (trigger/preset/duration/delay/easing/repeat/direction/intensity/random-offset, add/delete) via new `effectCommands.ts`/`animationCommands.ts`. A new **Performance Inspector** (`PerformanceInspectorPanel.tsx`, opened from a new "Performance" TopBar button) reports total/animated-layer counts, each effect layer's real tier-capped approximate particle count, large-asset count (≥2MB), and the shared design-warning feed — worded throughout as approximate counts, explicitly never a measured frame rate or hardware benchmark. Reduced-motion and performance-tier are now real, stateful controls in the main status bar (previously `hostSettings` was a hardcoded constant reachable only inside Behaviour Mode's own separate simulator) — "a clear per-project preview" now applies to the actual page content in Design/Preview mode, not just Behaviour Mode's simulated one. 20 new studio tests (283→303). **Three real bugs caught and fixed by this phase's own tests, not deferred:** (1) moving master-inheritance resolution out of `PageStage` and into `ThemeRenderer` (needed so the effect-layer cap could be computed once, before rendering) initially broke the circular-master-chain fallback, since the resolution call ended up outside `RenderErrorBoundary`'s actual React descendant tree — caught immediately by the pre-existing `ThemeRenderer.test.tsx` regression suite, fixed by introducing a `RenderedStage` descendant component mirroring the established `ThrowRenderError` pattern; (2) a `document` variable-shadowing bug in `PageStage.tsx` — the `RenderableDocument` context value is also (necessarily) named `document`, and a leftover `stageStyle(document.canvas)` call silently started reading the browser's global `Document` instead once the surrounding code was refactored — caught by inspection before it ever reached a test; (3) `usePlaybackGate`'s initial focus state relied on `document.hasFocus()`, which defaults to `false` in the very environment used to test it (and is unreliable across some embedding contexts generally) — caught by a dedicated test asserting the gate starts active, fixed by defaulting to optimistically focused and reacting only to real `blur`/`focus` events. Full workspace: `pnpm -r typecheck`, `pnpm -r test` (613 tests: theme-sdk 144, theme-renderer 166, studio 303), `pnpm run lint`, `pnpm run check:boundaries`, and `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. Both `@fdraft/theme-sdk` and `@fdraft/theme-renderer` rebuilt and `pnpm pack`-verified (the packed `theme-renderer` tarball's `package.json` correctly pins `"@fdraft/theme-sdk": "0.1.0"`, not `workspace:*`) — ready for the repository owner to cut real releases whenever they choose; version numbers were not bumped unilaterally, the same standing deferral as every prior phase. **Actually launched and visually verified** via the established in-memory-platform-plus-headless-Chromium method: added snow, confetti, and film-grain effect layers simultaneously to the FDraft Default Event template and watched real particle motion between two screenshots taken 800ms apart (a snowflake visibly fell and drifted); confirmed confetti renders as rotating pink rectangles and film grain as an animated noise texture, exactly matching each kind's configured shape/colour; added a `pulse`/`infinite` animation to a text layer; switched the performance tier to Low in the status bar and confirmed every effect vanished completely (canvas elements removed from the DOM, not just paused); opened the Performance Inspector and confirmed it reported 32 layers, 1 animated layer, 3/4 effect layers allowed at High with real per-effect particle-count estimates, and — most importantly — 18 real warnings correctly flagging the newly-added full-bleed snow layer for sitting above every protected component beneath it. Zero console errors throughout. `../FDraft` confirmed untouched via `git status` both before and after this phase. **Deferred, honestly incomplete:** Studio has no UI for hand-authoring a *custom* keyframe list (the schema, evaluator, and CSS generation all support up to 12 keyframes and are tested directly, but `PropertiesPanel`'s Animations section only offers the 10 presets — a real, meaningful gap, not a rounding error); no visual/drag-based keyframe timeline editor was attempted even for that reason; live click-driven `restartAnimation`/popup/navigation dispatch inside Studio's own preview remains unwired, the same standing deferral Prompt 8 already documented, now also covering animations specifically (continuous `whileTrue`-driven animations, including real hover, are fully live); no automated wall-clock performance-timing budget assertions were added (deliberately — timing-based test assertions are notoriously flaky on shared CI runners) — instead the *structural* caps (particle-count ceilings, effect-layer ceilings, DPR ceilings) are the enforced budget, verified with deterministic unit tests rather than timing measurements; high-DPI handling reads and caps `devicePixelRatio` and is unit-tested, but was not verified against an actual physical high-DPI display, only a headless Chromium run at DPR 1; audio was deliberately not touched anywhere, per the prompt's own explicit instruction. |
| 10. FDraft runtime integration | FDraft | Implemented, deliberately scoped core (verified read-only from FDraft-Studio) | Committed and pushed in the `FDraft` repository: commit `5b13a84` ("feat: integrate released FDraft theme runtime (Prompt 10)") on branch `feature/fdraft-theme-runtime`, `origin`-tracked, working tree clean at time of verification. Pins `@fdraft/theme-sdk@0.1.0`/`@fdraft/theme-renderer@0.1.0` as exact, checksum-verified `theme-runtime-v0.1.0` release tarballs (this repository's own release — see the "Release: `theme-runtime-v0.1.0`" section above). Builds a compatibility handshake (`src/infrastructure/theme-runtime/compatibility.ts`: `FDRAFT_SUPPORTED_COMPONENT_KEYS` = `page-title`/`event-information`/`event-countdown`/`draft-controls`/`film-grid`/`event-progress`/`points-counter`; `FDRAFT_SUPPORTED_CAPABILITIES` = `responsive`/`masters`/`popups` only, deliberately excluding `animations`/`behaviour`/`effects`), real host adapters for those 7 component keys wrapping existing FDraft components, safe `.fdtheme` loading with an outer error boundary falling back to normal FDraft on any theme failure, a dev-only `/theme-preview` route + 1000ms mtime-poll reload protocol (`GET /api/theme-preview?path=<absolute path>` + `GET /api/theme-preview/watch?path=`, both confirmed 404 in a real production build), and the `theme-projects/<slug>/` (editable `project.json` source) vs. `src/theme-packs/<slug>/theme.fdtheme` (compiled, packed output) directory boundary, proven with one real fixture (`fdraft-integration-fixture`) compiled/packed/loaded/rendered end to end. Per the commit's own evidence: full FDraft test suite 190 files / 2036 tests, lint/typecheck/build clean, real headless-browser visual inspection catching and fixing three bugs before acceptance. See `../FDraft/docs/fdraft-theme-runtime/INTEGRATION.md` for FDraft's own full evidence and rollback instructions — not duplicated here. **This row records read-only verification from FDraft-Studio, not work performed by this repository or this session** — no FDraft-Studio commit produced any of it. |
| 11. Simulator, live preview, and publishing | FDraft-Studio | Implemented, deliberately scoped core | Built the full five-part scope as five independently tested phases — see "Deferred" below for the honest gap list. **Phase A — Event-state simulator:** new, additive `SimulationScenario` schema (`packages/theme-sdk/src/schema/simulation.ts`) capturing every field the prompt lists (event status/active/available, opted-in, draft-generated, progress/watched/target counts, completed, current page/popup, all 3 performance tiers, reduced motion, simulated date/time, placeholder values, and a closed `dataProfile` enum — `normal`/`empty`/`loading`/`error`/`longTitle`/`maxFilmCards` — for representative mock data) on `StudioProjectDocument.simulationScenarios` (defaults to `[]`, no migration, same additive pattern as every prior phase); 8 curated built-in scenarios (`apps/studio/src/simulation/defaultScenarios.ts`) merged alongside project-saved ones, following the `starterTemplates.ts` fixed-list precedent. A shared `SimulationPanel` (state-editing form + scenario picker with save/update/rename/duplicate/delete) and `BehaviourTracePanel` (extracted from `BehaviourWorkspace`) are now used identically by Behaviour Mode's own simulator and a new, dedicated **Simulate** shell mode — both derive `RenderState`/`HostSettings` through one shared `simulationState.ts`, so the two can never drift into offering different controls for the same thing. **Phase B — Copy review:** `apps/studio/src/copyReview/copyReviewFindings.ts` — pure, scenario-aware detection of 4 of 5 categories (falling back to FDraft default, missing/blank-required, unresolved `{{placeholder}}`, inaccessible/empty-with-no-fallback-name) across every page/popup (via real master-inheritance resolution) × every declared copy slot × every default/saved scenario × every declared copy variant, all built on the one shared `resolveComponentCopy` — never a second resolution rule. `clipped` is a real, live measurement pass (`detectClipping.ts`'s `hasTextOverflow`, real `scrollWidth`/`clientHeight` vs. `clientWidth`/`clientHeight`, meaningless in jsdom by design — only a real browser layout engine proves it) — "Scan for clipped text" mounts every page/popup off-screen at 3 viewport profiles at once (each in its own wrapper so duplicate `data-fdraft-layer-id`s across copies never collide) and measures synchronously via `flushSync`. Documented scope limit: the live scan checks currently-authored copy, not every variant's wording rendered live. New "Copy review" modal (category filter, "Go to" navigation) from the TopBar. **Phase C — True preview mode:** extended `StudioShell`'s existing zero-chrome preview branch (not rebuilt) with a ‹ Desktop/Laptop/Mobile › viewport-profile cycler (`apps/studio/src/ui/viewportProfiles.ts`, shared with Copy Review) that actually resizes the rendered stage; deliberately did *not* add a scenario picker into Preview mode's own chrome (the prompt asks only for "cycle viewport profiles" + "return to editing" there — a full scenario picker already lives in Simulate mode), verified with a test asserting zero buttons exist inside the render target's own subtree. **Phase D — Open in FDraft + hot reload:** `apps/studio/src/devPreview/devPreview.ts` — compiles/validates exactly like Export (`analyzeThemeExport`), writes to a Studio-managed temp path (`<appDataDir>/dev-preview/<projectId>.fdtheme`, never inside FDraft), and *leaves the last good file untouched* on an invalid edit (never overwrites good with broken); `checkFDraftReachable` is a plain outbound probe with an `AbortSignal` timeout — Studio never opens a listener, so nothing here is reachable beyond the local machine regardless of the URL typed in. New "Preview in FDraft" modal auto-builds on open and after every successful save (not every keystroke — a `dirty: true→false` transition, matching "hot reloads after successful saves" literally), shows connection status, and surfaces the exact absolute path plus FDraft's own dev-preview page URL with one-click clipboard copy — built against FDraft's real, now-committed protocol (`GET /api/theme-preview?path=` + 1000ms-poll `watch` route, confirmed in Phase 10's evidence above). **Phase E — Link and publish to a local FDraft repository:** `apps/studio/src/publish/` — `fdraftLink.ts` (local-machine-only linked-repo path, keyed by project id, deliberately never part of the portable `.fdstudio` document); `fdraftRepositoryCheck.ts` (best-effort plausibility: `package.json` name/`@fdraft/*` dependency, `src/app` marker — "candidate until independently verified," same caveat as `RENDERER_HOST_NOTES.md`); `fdraftCompatibility.ts` (reads FDraft's actual committed contract — `installed-versions.generated.ts`/`compatibility.ts`, confirmed real and committed in Phase 10 above — no separate JSON manifest exists to read, so this parses the two real TS files directly; mirrors FDraft's own `checkThemeCompatibility` logic exactly, giving this feedback *before* publish); `publishDirectorySwap.ts` (atomic temp-dir-then-rename swap per directory, keeping exactly one generation of `.previous` backup for an explicit "Undo this publish" rollback — distinct from crash-safety, which is ephemeral); `publishToFDraft.ts` (`planPublish` — pure preview: slug from the project name with independent `isPathSafeSlug` defense-in-depth against path traversal, compile+validate, compatibility check, add/change/remove diff for both `theme-projects/<slug>/` and `src/theme-packs/<slug>/theme.fdtheme`, slug-collision detection against a *different* project's `metadata.id`; `executePublish` — the actual, confirmed write; never runs `git`, only returns plain-text `git status`/`git add`/`git commit` command text). New "Link FDraft Repo" and "Publish to FDraft" TopBar entries; a slug collision is the one blocker that can be overridden, and only behind a distinct, explicit "I understand this will overwrite a different project" checkbox — every other blocker (invalid project, incompatible with FDraft's real supported set, FDraft's integration files missing/unparseable) is never bypassable from the UI. `pnpm --filter @fdraft/studio test` (53 files, 412/412 tests, up from 303 at the end of Prompt 9) exercises all five phases, including a full Link→Plan→Publish→verify-on-disk round trip driven through the real `StudioShell` UI (directory-picker dialog queued, not mocked away) and a real `checkThemeCompatibility`-equivalent rejection of a project using an unsupported capability. Full workspace: `pnpm -r typecheck`, `pnpm -r test` (731 tests: theme-sdk 153, theme-renderer 166, studio 412), `pnpm run lint`, `pnpm run check:boundaries`, `pnpm --filter @fdraft/theme-sdk check:schemas`, and `pnpm --filter @fdraft/studio build`/`pnpm --filter @fdraft/renderer-lab build` all pass clean. **Not yet done:** a real end-to-end run against the actual live `../FDraft` checkout (dev server actually running, Studio actually pointed at it, a real publish actually landing in `../FDraft/theme-projects/`/`src/theme-packs/`) — the automated suite proves the mechanism against synthetic FDraft-shaped fixtures in temp directories, consistent with never mutating the real sibling repository without the user separately approving that specific action; this is the recommended next manual verification step. **Deferred, honestly incomplete:** Copy Review's live clipping scan checks only the currently-authored copy per slot, not every declared variant rendered live (would need constructing a modified project per variant); no visual/drag-based way to compare all three viewport profiles side by side at once (only one at a time, cycled); the dev-preview `baseUrl` isn't persisted across sessions (`@tauri-apps/plugin-store` is installed but not yet wired to anything); publish's plausibility check is best-effort text/marker matching, not a guarantee. |
| 12. Cross-repository hardening and installers | Both | Implemented, deliberately scoped core | Before writing any code, three parallel read-only audits established exactly what of this prompt's very large "quality and security" scope was already covered by earlier phases (CLAUDE.md has mandated malformed-package/SVG/archive-traversal/crash/interrupted-save testing since Prompt 1) versus a genuine gap — targeted only the real gaps rather than re-verifying or rebuilding what already worked. **Confirmed already solid, untouched by this phase:** malicious-archive tests (zip-slip, dangerous extensions, file-count, compression-ratio), malicious-SVG tests, path-traversal tests outside archives, and atomic-save interruption/recovery including a real crash-mid-swap simulation. **Phase A (small precise gaps):** a direct test for `MAX_TOTAL_UNCOMPRESSED_BYTES`/`ARCHIVE_TOO_LARGE` (wired but previously untested); direct `checkSvgSafety` assertions for `no-use-external`/`no-css-import`/`no-entity-expansion` (previously only proven indirectly); UNC path (`\\server\share\...`) tests for `pathUtils.ts` (code already handled this, just never exercised). **Phase B (fuzz/property testing, a genuine gap — no property-based library existed anywhere in the workspace before this):** added `fast-check`; property tests for `SimulationScenarioSchema`/`BehaviourRuleSchema` round-trips and rejection invariants in `theme-sdk`, and for `evaluateCondition`/`resolveActiveBehaviourRules` in `theme-renderer` (never throws, always returns a well-formed shape, a disabled rule never wins a contested target — checked against generated trigger/condition/action trees, not hand-picked fixtures). **Phase C (realistic-scale test, also a genuine gap):** a project with 25 pages × 14 layers (350 layers) and 10 distinct assets validates/compiles/packs successfully, with a determinism check (identical bytes on re-pack) — no timing assertions, matching this codebase's established aversion to flaky CI checks. **Phase D (Windows-specific error handling):** `isMissingPathError` only recognized one failure category before this phase; added `isFileLockedError` (EBUSY/"resource busy or locked") and a **proactive** `assertPathNotTooLongForWindows`/`exceedsWindowsMaxPath` check (rejects *before* any write is attempted, not just pattern-matching the OS error after a partial write) wired into `ProjectSession.saveAs` and `publishToFDraft`'s `planPublish` (a new `pathTooLong` blocker, consistent with the existing blocker-not-exception pattern there). **Phase E (Studio's own UI accessibility, a genuine gap — none of Studio's 7 `role="dialog"` modals trapped focus or closed on Escape, confirmed by direct inspection):** one shared, tested primitive (`apps/studio/src/ui/useModalA11y.ts` — focus-trap, initial focus, Escape-to-close, focus-restore-on-close) wired into all 7 (`ValidationPanel`, `PerformanceInspectorPanel`, `CopyReviewPanel`, `DevPreviewPanel`, `LinkFDraftRepositoryDialog`, `PublishToFDraftPanel`, `ExportDialog`) instead of seven divergent implementations; a `:focus-visible` replacement for the canvas viewport's previously-bare `outline: none`; a global `:focus-visible` base rule and a defensive blanket `prefers-reduced-motion` reset for Studio's own chrome (nothing animates there yet — forward-defense, not a fix for a live problem); 5 new RTL tests using `userEvent.tab()`/`keyboard()` (not just `click()`) proving the focus trap, Tab-wrap in both directions, Escape-close, and focus-restore all genuinely work. **Not attempted, explicitly:** real screen-reader software testing (none available in this environment — the practical proxy is the pre-existing aria-attribute coverage plus this phase's keyboard-only work); new Studio-chrome contrast-auditing infrastructure (the existing `contrastRatio` math stays theme-content-only, as it always has been). **Phase F (CI):** `.github/workflows/ci.yml` split from one monolithic `build-and-test` job into six independent jobs (`workspace-checks` incl. a new `pnpm audit --audit-level=high` gate, `sdk`, `renderer` needs `sdk`, `renderer-lab` needs `renderer`, `studio-frontend` needs `renderer`, `studio-tauri-rust` fully independent — no Node/pnpm setup needed at all now) — a Studio failure no longer marks an unrelated SDK job red, and `studio-tauri-rust` needs no JS toolchain to start failing on a Rust-only problem. YAML validated with `js-yaml`. `pnpm audit` run directly: **0 vulnerabilities at any severity**, 304 total dependencies. **Phase G (Windows Studio installer):** confirmed via direct investigation (not assumed) that cross-compiling a Windows Tauri/NSIS bundle from macOS is genuinely infeasible in this toolchain — fails compiling the `ring` crate for lack of a Windows C toolchain, the exact same finding FDraft's own `docs/product-spec.md` already recorded for the identical Tauri v2 stack. Added `publisher`/`copyright`/`category`/`shortDescription`/Windows `nsis.installMode` to `apps/studio/src-tauri/tauri.conf.json` (previously absent; identifier `com.fdraftstudio.app` and product name "FDraft Studio" were already distinct from FDraft's own Tauri app, and `icon.ico` was already a real, valid multi-resolution icon) — verified with `cargo check`/`fmt --check`/`clippy -D warnings`, all clean, confirming the config still parses correctly. New `.github/workflows/release-studio-windows.yml` (tag-triggered on `studio-v*.*.*`, `runs-on: windows-latest`, mirroring FDraft's own proven `tauri-apps/tauri-action` pattern — tag-vs-version check, the same frontend gate `ci.yml` runs, then a real native build+release) — YAML-validated. Deliberately **excludes** any `plugins.updater`/`createUpdaterArtifacts` (FDraft's own updater is FDraft-specific — its own signing key, its own release endpoint — reusing it would couple the two applications' release processes, explicitly avoided per this prompt's own instruction). Code-signing is documented as unavailable (no certificate), the same honest note FDraft's own docs already use for the identical situation — never claimed as solved. **Not attempted:** actually triggering this workflow (needs a real tag push) or verifying the resulting `.exe` on a real Windows machine — both require explicit sign-off given they're real, hard-to-reverse CI/release actions, so this stays the one open item below. **Phase H (prove FDraft's production bundle excludes Studio code):** already structurally true (FDraft's `package.json` has zero `file:`/`link:`/Studio references) but never verified against FDraft's actual *build output* before this phase — built FDraft's real production bundle (`next build && serwist build`, read-only with respect to FDraft's tracked files — confirmed via `git status`/`git check-ignore` both before and after) and grepped `.next/static`, `.next/server`, and `public/sw.js` for any Studio-specific identifier: **zero hits**. Additionally confirmed the installed `@fdraft/theme-sdk`/`@fdraft/theme-renderer` packages resolve via pnpm's content-addressed store keyed by a hash of the tarball URL itself (`node_modules/@fdraft/theme-sdk` → `../.pnpm/@fdraft+theme-sdk@https+++github.com+Burrichen+FDraft-Studio+...`), never a symlink or path reference to the local `../FDraft-Studio` checkout. **Phase I (documentation):** new `docs/guides/{USER_GUIDE.md, TROUBLESHOOTING.md, THEME_AUTHORING_RULES.md, FORMAT_AND_COMPATIBILITY.md, RELEASE_CHECKLIST.md}` — `RELEASE_CHECKLIST.md` is the genuinely new piece (a consolidated, step-by-step cross-repository promote/pin/rollback/compatibility-check runbook; the prior docs covered the mechanics and a rollback concept but not one connected checklist), the other four are plain-language/developer-facing references matching the actual current UI and error messages, not aspirational ones. Reviewed FDraft's own `docs/fdraft-theme-runtime/INTEGRATION.md` for a genuine gap per this prompt's "only... FDraft-specific... information, do not duplicate" instruction — found none (it already covers install references, protocol, directories, adapters, and rollback in real depth) — so, per the plan's own "no speculative additions" rule, **FDraft's docs were not touched**. Full workspace after all phases: `pnpm -r typecheck`, `pnpm -r test` (**768 tests**: theme-sdk 168 [+15 vs. end of Prompt 11], theme-renderer 171 [+5], studio 429 [+17]), `pnpm run lint`, `pnpm run check:architecture`, `pnpm run check:boundaries`, `pnpm --filter @fdraft/theme-sdk check:schemas`, `pnpm --filter @fdraft/studio build`, `pnpm --filter @fdraft/renderer-lab build`, `cargo check`/`fmt --check`/`clippy --all-targets -- -D warnings` (from `apps/studio/src-tauri`), and `pnpm audit --audit-level=high` all pass clean. **Deferred, honestly incomplete — the one real blocker:** the Windows installer workflow is written and reasoned through carefully against FDraft's own already-proven, identical template, but has never actually been run — "does it produce a genuinely working, installable `.exe`" is confirmed only by triggering `release-studio-windows.yml` (a real tag push) and testing the result on a real Windows machine, neither of which has happened yet. Also still open from Prompt 11 and unaffected by this phase: real end-to-end verification of the dev-preview/publish workflows against the actual live `../FDraft` checkout (dev server running, a real publish landing on disk there) remains a manual step, not yet performed. |
| 13. Official event starter projects | Both | Implemented, deliberately scoped core | Built Halloween, Christmas, and "F* You, It's January!" (not Carnival, per the prompt's own instruction) as real, editable `.fdstudio` projects and compiled `.fdtheme` packs — never hand-authored project JSON. **New pattern, not a shortcut:** `apps/studio/scripts/starterEventBuilder.ts` is a shared driver of thin wrappers around real, already-tested Studio functions (`ProjectSession`, the real asset-import pipeline, real `Command<StudioProjectDocument>` factories) — genuinely "the same tool a user would use," driven headlessly because no interactive GUI session exists in this environment (confirmed via search this is a new pattern: the one prior fixture generator, `theme-sdk/scripts/write-sample-fixture.ts`, writes hand-authored JSON directly). Three per-event drivers (`scripts/build-{halloween,christmas,january}.ts`) each import real artwork, compose pages/popups/components, set deliberate copy on every real slot, save simulator scenarios, validate, save, compile, and attempt a real publish. **A real, previously-undocumented compatibility gap was found by actually running this, not predicted in advance:** the "FDraft Default Event" template (`createFdraftDefaultEventProject`, Prompt 7) places 14 distinct component keys across its 8 pages, but FDraft's real, currently-committed adapters (`FDRAFT_SUPPORTED_COMPONENT_KEYS`) only cover 7 — *any* project built from that template fails FDraft's real compatibility check on component keys alone, independent of capability usage. This is a genuine dogfooding finding, recorded here rather than routed around; no FDraft adapter code was touched to "fix" it (out of this prompt's scope, per the plan's own "what this plan does not do"). **Halloween** (11 real assets from `../FDraft/public/events/halloween/`): built from the full 14-key default template (per the prompt's page/popup list); a real, editable `ImageStateGroup` ("Candy Bowl": empty/low/medium/full using the 4 real PNGs) driven by 4 real `BehaviourRule`s (priority-0 default → Empty, 3 priority-1 `inRange` rules over `progressPercent` at 25-49/50-74/75-100 → Low/Medium/Full — continuous, gap-free, boundary-exact, editable in Behaviour Mode, never hard-coded domain logic); static Pumpkin/Gravestone/Gravestone-Moss image layers; Ghost in a new "Event Available"/Join popup; deliberate copy on every real slot across all 8 pages + popup; 5 saved simulator scenarios (candy-bowl progress boundaries + event-ended). `validateProject`: valid=true, 3 non-blocking design warnings (`DECORATIVE_LAYER_COVERS_COMPONENT` — the three bottom-of-page decorative images sit above the default template's `event-navigation` bar; not fixed, since it doesn't block validation or the scoped goal, and is recorded honestly rather than hidden). Per the user's own explicit choice ("build it correctly, document the block") publish was attempted for real and **correctly, honestly BLOCKED**: `"Uses component(s) FDraft doesn't support yet: challenge-card, complete-watch-action, draft-progress, event-navigation, event-points-counter, generate-draft-action, profile-badge, results-completion-content." "Uses capability/capabilities FDraft doesn't support yet: behaviour."` — a real proof the safety mechanism works, not a bug. **Christmas** (8 real assets from `../FDraft/public/events/christmas/` — FDraft's own `public/events/README.md` calls this set "a scaffold only — placeholder-quality... no Christmas event logic, page, or nav entry wired up anywhere yet," recorded honestly in the project rather than presented as final art): built from a **custom** page/component structure (`session.newProject` + rename, not the default template) declaring only the 7 real FDraft-supported component keys, specifically *because* the 14-vs-7 gap above would otherwise have blocked it too — 6 pages + Join popup, real imagery placed decoratively, full copy pass, 4 saved simulator scenarios. `validateProject`: valid=true, 0 design warnings. **Published for real, end to end**: `theme-projects/christmas/project.json` (+8 real content-addressed asset files) and `src/theme-packs/christmas/theme.fdtheme` atomically written into the real `../FDraft` checkout — confirmed via `git -C ../FDraft status --short` showing exactly `?? src/theme-packs/christmas/` and `?? theme-projects/christmas/`, nothing else touched. **January** ("F* You, It's January!" — no real art exists anywhere for this event, confirmed via exhaustive search before starting: no `public/events/january/`, the one January-adjacent file has empty `assets`/`layouts`): same compatibility-scoped 7-key structure as Christmas (6 pages + Join popup); grey/miserable mood built entirely from 2 new design tokens + full-canvas background shape layers (no image assets needed); real, procedural `clouds`/`rain`/`fog` effect layers (via `addEffectLayer`, sent to the back of the stack via `buildZOrderCommand` so an ambient full-bleed effect never sits above interactive components — this fixed a real `DECORATIVE_LAYER_COVERS_COMPONENT` warning found by actually running the build, not predicted); a clearly-labeled, visibly-named "MISSING ART — Scattered rubbish (needs illustration)" placeholder shape layer, since no real asset exists and none was invented; the 25-31 January window, film eligibility, whitelist, and Misery Points are never encoded — only presentation, exactly as required; 5 saved simulator scenarios named exactly as the prompt specifies (`available`, `joined`, `partial progress`, `completed`, `ended`). `validateProject`: valid=true, 0 design warnings. Publish attempted and **correctly, honestly BLOCKED**, this time on a single, clean reason (no component-key mismatch, since January uses the same 7-key structure as Christmas): `"Uses capability/capabilities FDraft doesn't support yet: effects."` **Phase 4 cross-event proof:** (1) *Delete-original-sources* (`scripts/verify-delete-sources.ts`) — copied Christmas's real source PNGs to a scratch directory (never touching the real files), built from the copy, deleted the copy entirely, then reopened the saved `.fdstudio` and recompiled it: `Reopened OK: 8 asset record(s), 8 byte blob(s) present in the package` and a successful recompile — proves `packFdstudio`'s content-addressed, self-contained embedding (confirmed by reading `saveProject`/`packFdstudio` in `src/project/projectFile.ts`) means a project never depends on its original source file continuing to exist. (2) *Viewport/reduced-motion/performance-tier coverage* — new, permanent test `apps/studio/test/starterEvents/simulationCoverage.test.tsx`: rebuilds all three events fresh (no dependency on prior scratch output), then renders every one of each event's saved `SimulationScenario`s (5+4+5=14 scenarios) at 3 viewport widths (390/1366/1920px) using the real Simulate-mode derivation chain (`scenarioToLiveState`→`deriveHostSettings`/`deriveRenderState`, the same functions the Simulation panel/Behaviour Mode/Preview mode all go through) and the real `createStudioComponentAdapterRegistry` — 42 render combinations, zero `theme-render-failed`/`layer-render-failed` markers, all 3 test cases pass. (3) *FDraft dev-preview verification, against the actual live `../FDraft` checkout, not a synthetic fixture* — started FDraft's real `next dev` server and drove its real, existing dev-only `/theme-preview` route (`src/app/(dev)/theme-preview/`, 404s in production) with Playwright (Chromium, already installed in FDraft's own `node_modules`): Halloween's real `.fdtheme` shows FDraft's real `"This theme isn't compatible with this version of FDraft"` message with the exact reasons, zero console errors, rest of the page fully usable; January's shows the single clean `effects`-only reason, same clean behavior; **Christmas genuinely renders** — real title ("A Cozy Christmas"), real countdown counting down live ("59m 58s"), real imported artwork placeholders — zero console errors. Screenshots captured for all three (not committed — ephemeral scratch evidence, per this session). This is the real end-to-end verification against the live checkout that Prompt 11's own row flagged as not yet done — see "Current blockers" below, now resolved. `pnpm --filter @fdraft/studio typecheck`/`lint` clean throughout (checked after every script addition, not just at the end). Full workspace: `pnpm -r typecheck`, `pnpm -r test` (**771 tests**: theme-sdk 168, theme-renderer 171, studio 432 [+3 vs. end of Prompt 12]), `pnpm run lint`, `pnpm run check:architecture`, `pnpm run check:boundaries`, `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. FDraft confirmed untouched except the one deliberate, correct Christmas publish (`git status` before/after). **Not done / deferred, honestly incomplete:** no FDraft adapter code was added for `behaviour`/`effects`/the missing 7 component keys — publishing Halloween and January stays genuinely blocked until FDraft's own team decides to close that gap, which is explicitly out of this prompt's scope; Halloween's 3 `DECORATIVE_LAYER_COVERS_COMPONENT` design warnings (decorative images over the default template's nav bar) were left as-is rather than repositioned, since they don't block validation and fixing Studio's own default-template layout wasn't part of this prompt's scope; Christmas's artwork is FDraft's own "placeholder-quality... scaffold only" set, not final creative, and is recorded as such in the project rather than presented as done; January's "scattered rubbish" imagery remains a labeled empty slot — no real asset exists and none was invented; no native OS screenshots were taken anywhere in this phase (Playwright against a self-started dev server only, per this project's established screenshot-safety policy). |

## Release: `theme-runtime-v0.1.0`

Cut by explicit repository-owner authorization ahead of Prompt 10 (the release boundary this
project's own blocker list had been flagging since the end of Prompt 9). Evidence:

- **Source commit:** `d1c876bc5b8ac784c801da450976386bc42d68a9` on `main` (the complete,
  audited Prompts 1–9 working tree, committed as a single accurately-described commit —
  314 files changed, 57761 insertions — since no prior commit in this repository's real
  history captured that work). Pushed to `origin/main` before tagging.
- **Tag:** annotated `theme-runtime-v0.1.0`, pointing at that exact commit; pushed to origin.
  Cross-verified: `git rev-list -n 1 theme-runtime-v0.1.0`, the tag object's own `object.sha`
  (via the GitHub API), `origin/main`'s HEAD, and the GitHub Release's `targetCommitish`
  (`main`) all resolve to `d1c876b...` — no drift between tag, release, and pushed source.
- **GitHub Release:** "FDraft Theme Runtime v0.1.0" at
  `https://github.com/Burrichen/FDraft-Studio/releases/tag/theme-runtime-v0.1.0`, one
  combined release for both packages as a tested compatibility pair (a deliberate,
  user-approved deviation from the per-package `theme-sdk-v<version>` /
  `theme-renderer-v<version>` tag scheme `docs/architecture/COMPATIBILITY_AND_RELEASES.md`
  originally documented — see that file's "Current status" for why).
- **Packages:** `@fdraft/theme-sdk@0.1.0` and `@fdraft/theme-renderer@0.1.0`, packed via
  `pnpm pack` (which correctly rewrote `theme-renderer`'s `"@fdraft/theme-sdk": "workspace:*"`
  to the exact resolved `"0.1.0"`). Assets attached to the Release: `fdraft-theme-sdk-0.1.0.tgz`,
  `fdraft-theme-renderer-0.1.0.tgz`, `SHA256SUMS.txt`, `compatibility-manifest.json` (machine-
  readable provenance: release tag, source commit, both package versions, theme/project format
  versions, Node/pnpm requirements, filenames, sizes, SHA-256 checksums, release timestamp).
  Checksums:
  ```
  74dd118b2ea92fe578d219e41e0b21bfdcb73daffa31e2752cbd9b40b729477c  fdraft-theme-sdk-0.1.0.tgz
  ddd6e1e57a74bbfb936d0547fcd6fddb1e5026531766e553303885dfb0b1f0e2  fdraft-theme-renderer-0.1.0.tgz
  ```
- **Format versions carried by this release** (from `packages/theme-sdk/src/schema/versions.ts`,
  independent of the packages' own semver): project format `1.0.0`, theme format `1.0.0`,
  minimum-supported project format `0.9.0`, minimum-supported theme format `1.0.0`,
  SDK/renderer contract version `0.1.0`.
- **Pre-tag verification actually performed** (not just asserted): both `.tgz` archives were
  inspected (runtime files, type declarations, `package.json`, LICENSE/README only — no dev
  fixtures, no source-tree ZIP, no `node_modules`); a fresh, out-of-workspace consumer project
  installed both packed tarballs via the committed `pnpm-workspace.yaml`/lockfile toolchain
  and ran a real functional smoke test — `createProject`/`validateProject`/`compileTheme`
  from `theme-sdk`, and `evaluateCondition`/`resolveActiveBehaviourRules`/a real
  `ThemeRenderer` React SSR render (`renderToStaticMarkup`, asserting authored copy text and
  the `data-fdraft-stage` marker both appear) from `theme-renderer` — all passed; the original
  tarball directory was then moved away and the smoke test re-run unchanged against the
  already-installed consumer, proving the install doesn't retain a live path back to the
  source tree.
- **Post-publish verification actually performed:** both assets plus `SHA256SUMS.txt` were
  downloaded back from the live, public Release via unauthenticated `curl` (no `Authorization`
  header — confirming the public-repo, no-auth distribution model actually works for a real
  external client, not just for the account that published it); `shasum -a 256 -c
  SHA256SUMS.txt` against the downloaded bytes reported `OK` for both; a second, independent
  fresh consumer installed and smoke-tested those exact downloaded bytes with the same passing
  result. Tag, Release `targetCommitish`, and `compatibility-manifest.json`'s `sourceCommit`
  all agree with the pushed `main` HEAD (see above).
- **Genuinely new finding, not previously documented:** a scoped package's own bare-semver
  internal dependency (`theme-renderer`'s packed `"@fdraft/theme-sdk": "0.1.0"`) makes pnpm
  attempt a public-registry resolution by default even when that same package is already
  present as a `file:`/URL-sourced top-level dependency in the consumer. Fix: the consumer adds
  an `overrides` entry in its own `pnpm-workspace.yaml` (not `package.json`'s `"pnpm"` field,
  which pnpm 11 no longer reads) pointing `@fdraft/theme-sdk` at the same tarball/URL source.
  Prompt 10's FDraft-side `pnpm-workspace.yaml` will need this same override. Written up in the
  Release's own notes; see `docs/architecture/COMPATIBILITY_AND_RELEASES.md` for the
  repository-level record.
- **Exact values for Prompt 10 to pin:**
  ```json
  "@fdraft/theme-sdk": "https://github.com/Burrichen/FDraft-Studio/releases/download/theme-runtime-v0.1.0/fdraft-theme-sdk-0.1.0.tgz",
  "@fdraft/theme-renderer": "https://github.com/Burrichen/FDraft-Studio/releases/download/theme-runtime-v0.1.0/fdraft-theme-renderer-0.1.0.tgz"
  ```
  plus the `pnpm-workspace.yaml` override above for `@fdraft/theme-sdk` pointing at the same
  SDK URL, and a checksum cross-check against the two SHA-256 values recorded here before
  either URL is added to FDraft's own lockfile.
- FDraft was not touched by this release — confirmed via `git status` in `../FDraft` before
  starting and not modified at any point during this task.

| 14. Close FDraft compatibility gaps | Both | Complete — investigation and shared-package finding here, real adapter/capability implementation in FDraft's own repository (commit `006035c`) | **Phase 1 — exact compatibility matrix**, built by direct inspection of `apps/studio/src/templates/fdraftEventContract.ts` (the real, only source of the 14-key default template), `packages/theme-renderer/src/componentAdapters/sampleAdapters.tsx`'s `SAMPLE_COMPONENT_KEYS`/`SAMPLE_COPY_CONTRACTS` (the shared copy-slot contract), and FDraft's real `compatibility.ts`/`component-adapters.tsx` (read, not assumed) — not the 7-vs-14 headline row 13 recorded, but the exact set, since FDraft's 7th supported key (`event-progress`) is never placed by the template at all (the template uses the distinct key `draft-progress` instead), so only 6 of FDraft's 7 keys actually overlap the template: <br><br>**Fully implemented (6):** `page-title`, `event-information`, `event-countdown`, `film-grid`, `points-counter`, `draft-controls` — all pre-existing FDraft adapters, all real component reuse.<br>**Partially implemented (1, pre-existing, unaffected by this row):** `draft-controls` reuses `DraftLifecycleView` wholesale but its `skipLabel`/`confirmLabel` copy-slot overrides are still not wired to real button text — a documented Prompt-10 gap, out of this row's scope.<br>**Missing before this row (8):** `generate-draft-action`, `profile-badge`, `event-navigation`, `draft-progress`, `complete-watch-action`, `challenge-card`, `results-completion-content`, `event-points-counter`. For each: required read-only host data, allowed visual properties (the template's fixed `FDRAFT_EVENT_ALLOWED_PROPERTIES` list — color/backgroundColor/opacity/fontSize/fontWeight/textAlign/borderRadius/padding/margin — applies uniformly, nothing component-specific), supported copy slots (from `SAMPLE_COPY_CONTRACTS`), fallback behaviour, and tests required were all recorded and handed to the FDraft-side implementation — see FDraft's own `docs/fdraft-theme-runtime/INTEGRATION.md` and its `component-adapters.tsx`/`compatibility.ts` for the final, real mapping each key actually got (a dedicated read-only research pass across FDraft's real components — `WatchToggle`, `ChallengeBrowser`, `EventEndingDialog`, `ProfileMenu`, `EVENT_DEFINITIONS`, `getAllBalances`, etc. — preceded every one of those choices; nothing was invented). <br><br>**`behaviour` and `effects` capability classification — the load-bearing finding of this row:** both are **already fully implemented, tested, and host-agnostically wired in the shared packages already released as `theme-runtime-v0.1.0`** — confirmed by direct code reading, not assumed from either capability's own doc comments: `packages/theme-sdk/src/schema/behaviour.ts`'s `BehaviourRuleSchema`/`BehaviourConditionSchema`/`BehaviourActionSchema`/`RuntimeVariableSchema` are closed `z.discriminatedUnion`/`z.strictObject` schemas that already safely reject any unknown variable/action/operator/malformed rule at parse time (verified via Prompt 12's own `behaviour.property.test.ts`); `packages/theme-renderer/src/behaviourResolve.ts`'s `resolveActiveBehaviourRules`/`resolveTriggeredRule` are deterministic, non-recursive, always-terminating, priority-then-declaration-order conflict resolvers (216 lines, 14+5 tests); `RendererContext.tsx` already calls `resolveActiveBehaviourRules(document.behaviourRules ?? [], renderState, hostSettings)` unconditionally on every render (not gated by any capability flag), and `ImageLayerView`/`ComponentLayerView` already read its `imageStateOverrides`/`styleOverrides`/`enabledOverrides`/`copyVariantOverrides` — this is the exact, real mechanism that already drove Halloween's Candy Bowl correctly inside Studio. Effects: `packages/theme-renderer/src/{particleEngine,performanceCaps}.ts` + `layers/EffectCanvas.tsx` already implement real, bounded `rain`/`clouds`/`fog` (and 8 other kinds) particle rendering — hard per-tier caps (`PERFORMANCE_TIER_CAPS`: `low` = 0 particles/0 effect layers/no motion, `medium`/`high` bounded and DPR-capped), `reducedMotion` → static resting frame, `usePlaybackGate` already pauses the animation loop when the document is hidden, the window loses focus, or the canvas scrolls offscreen (via `visibilitychange`/`blur`/`focus`/`IntersectionObserver`), and a null `getContext` result fails safely (`if (!ctx) return`) rather than crashing. `packages/theme-sdk/src/compile/compileTheme.ts`'s `detectCapabilities` already correctly derives `behaviour`/`effects` into a compiled theme's manifest from real project content (`behaviourRules.length > 0` / any `effect`-type layer) — confirmed this is exactly why Halloween's and January's manifests declared them in row 13. <br><br>**Conclusion: the entire gap for both capabilities belongs to FDraft's own host layer, not the shared SDK/renderer** — specifically (a) `FDRAFT_SUPPORTED_CAPABILITIES` simply didn't list them, and (b) FDraft's only real `<ThemeRenderer>` call site (`theme-preview-client.tsx`) passed neither a `hostSettings` nor a `renderState` prop at all, so even with the capability declared, `RenderState.event` would stay entirely `undefined` and every Behaviour condition would silently evaluate false (a safe no-op, per `evaluateCondition`'s own "missing value never throws, only reads as not-equal" rule — never a crash, but also never the theme-authored behaviour actually working). Also confirmed no SDK schema change is needed for the 8 new component keys: `ComponentRequirement.componentKey`/`ComponentLayer.componentKey` are both plain `z.string().min(1)` in the schema, not a closed enum — a host has always been free to support any key string without any SDK change. <br><br>**Phase 2 outcome: no shared package changes were made, and no new release was cut** (per this row's own explicit "if none needed, record and continue" instruction) — `@fdraft/theme-sdk`/`@fdraft/theme-renderer` both stay at the already-released, checksum-verified `0.1.0` (`theme-runtime-v0.1.0`); FDraft's existing pin is untouched. This is a genuine finding, not a shortcut: verified by reading the actual shipped source of both packages, not by re-reading their own doc comments' claims. **Phase 3, completed in FDraft** (branch `feature/fdraft-theme-runtime`, commit `006035c` — "feat: implement real adapters for all 14 default-template components + Behaviour/effects", pushed): `FDRAFT_SUPPORTED_COMPONENT_KEYS` now lists all 14 (real adapters for `generate-draft-action`/`profile-badge`/`event-navigation`/`draft-progress`/`complete-watch-action`/`challenge-card`/`results-completion-content`/`event-points-counter`, added to the original 6 in-template keys — `event-progress` stays supported too, an FDraft key the template itself never places); `FDRAFT_SUPPORTED_CAPABILITIES` now includes `behaviour`/`effects` (added only after a new test proved a real `BehaviourRule` and a real `rain` effect layer render correctly through FDraft's real adapters at every performance tier — never flipped merely to pass validation); a real, previously-undiscovered semantic bug was found and fixed while wiring this (`points-counter` was reading the event-scoped balance instead of the profile's real overall/Lifetime balance) and `theme-preview-client.tsx` now passes real `hostSettings`/`renderState` to `ThemeRenderer` for the first time (previously neither was passed, so a declared-supported theme's Behaviour rules would have silently no-op'd) plus a real bug fix (a failed reload used to wipe the last successfully loaded theme instead of keeping it visible, contradicting `docs/architecture/INTEGRATION_WORKFLOW.md`'s own documented "last valid preview remains active" contract). **Verified against FDraft's real running dev server, not just tests**: Halloween and January were rebuilt fresh via this repository's own `apps/studio/scripts/build-{halloween,january}.ts` and loaded through the real `/theme-preview` route — both now load successfully (previously blocked), Halloween's Candy Bowl visibly changes from Low to Full as the dev-preview's new simulator moves progress from 40% to 100%, and January's rain/clouds/fog render as three real canvas elements — zero console errors in all three (Halloween/January/Christmas). FDraft's full suite: 2061/2061 tests pass, typecheck/lint/format:check all clean. Package pins genuinely untouched (`@fdraft/theme-sdk`/`@fdraft/theme-renderer` still exact `theme-runtime-v0.1.0` tarball URLs, matching this row's own Phase 2 finding that no shared-package change was needed). **Deliberately not done**: Halloween's and January's runtime output was not published into `theme-projects/`/`src/theme-packs/` — publishing them is a separate action for this repository's own Studio publish workflow (Prompt 13's own scripts), not part of this compatibility-gap-closing task; `animations` stays undeclared (not exercised by this phase). See FDraft's own commit for its full test evidence and remaining-gaps list — not duplicated here, per this project's own established "don't duplicate the other repository's evidence" convention (row 10). |

| 15. Redo official-event dogfooding against the closed gap | Both | Complete — all three official events now publish and render without compatibility exceptions or reduced structures | **Refreshed capability info, verified live**: `apps/studio/src/publish/fdraftCompatibility.ts`'s `readFDraftCompatibility` (a live regex-read of FDraft's real committed `compatibility.ts`/`installed-versions.generated.ts` — never cached in Studio) was invoked directly against the linked `../FDraft` checkout: `installedSdkVersion`/`installedRendererVersion` both `0.1.0`, `supportedComponentKeys` now all 15 (the 14 default-template keys plus `event-progress`), `supportedCapabilities` now `responsive`/`masters`/`popups`/`behaviour`/`effects`. `checkProjectAgainstFDraft` against the full 14-key template + `behaviour`+`effects` returned `{compatible: true, reasons: []}` — confirmed via Studio's own real mechanism, not assumed. **Halloween**: unchanged structurally (it already used `startFromDefaultTemplate`, the full 14-key template, since row 13 — only the header comment describing the *expected* publish outcome was corrected). **Christmas and January**: migrated off the temporary compatibility-scoped 7-key custom structure onto `startFromDefaultTemplate` (all 14 keys, 8 pages), matching Halloween's own pattern — a new "Event Available" popup was added to both (mirroring Halloween's), and deliberate copy was written for all 8 newly-placed component keys per event, in each event's existing tone; every previously-existing real decorative image, effect layer (January's `rain`/`clouds`/`fog`), grey palette, the honest "MISSING ART — Scattered rubbish" placeholder, and all saved simulator scenarios were carried over byte-for-byte unchanged (verified: `Christmas — migrated off the temporary 7-key structure, same visual intent preserved` and the January effects/placeholder tests in the updated permanent suite, below). A genuine, previously-unexercised path was found and fixed while doing this: `executePublish` never re-checks `plan.blocked` itself (by design — the gate lives in the caller, per its own doc comment), so a `slugCollision` on re-publishing the SAME conceptual Christmas project under its migrated structure needed the same explicit "I understand this will overwrite a different project" gate `PublishToFDraftPanel.tsx` already implements — added as `confirmSlugOverwrite` to `saveCompileAndReport`, defaulting to requiring explicit opt-in, never silently bypassing the safety check. **Real publish, this time succeeding for all three**: `Halloween: PUBLISHED — theme-projects/halloween, src/theme-packs/halloween`; `Christmas: PUBLISHED — theme-projects/christmas, src/theme-packs/christmas` (migrated in place, one `.previous` backup kept locally, not committed); `January: PUBLISHED — theme-projects/f-you-it-s-january, src/theme-packs/f-you-it-s-january` (the real slug, correctly derived from the project's real approved name "F* You, It's January!" via `slugify` — never forced to a literal "january" to match the architecture doc's own illustrative placeholder). **Updated permanent cross-event test** (`apps/studio/test/starterEvents/simulationCoverage.test.tsx`, 14 tests): rebuilds all three fresh against a synthetic, real-shaped FDraft fixture repo (mirroring `test/publish/publishToFDraft.test.ts`'s own established convention — hermetic, no sibling-repo dependency, portable to CI) with a byte-for-byte copy of FDraft's real current `compatibility.ts` content; asserts every event's build output contains `PUBLISHED` and never `BLOCKED` — **a blocked publish is now a test failure, not an accepted/documented outcome**, replacing row 13's own honest-but-now-stale acceptance of Halloween/January's blocks. Also asserts: every scenario × viewport combination renders with no error marker (as before); a NEW performance-tier (low/medium/high) × reduced-motion (on/off) matrix per event, independent of any one scenario's own stored values; Halloween's Candy Bowl resolves the exact right image state at all 8 documented progress boundaries (0, 24, 25, 49, 50, 74, 75, 100) via a direct `resolveActiveBehaviourRules` call against the compiled document's real `behaviourRules`; January's `rain`/`clouds`/`fog` are all three present as real effect layers and render without failure across every tier/reduced-motion combination; January's missing-art placeholder is still present (never silently dropped); Christmas now places all 8 previously-missing component keys and still shows its original 6 decorative image layers, unchanged. **Real end-to-end verification against the live FDraft dev server** (not just tests): started a real `next dev`, drove the real `/theme-preview` route with Playwright for all three real, now-published `theme.fdtheme` files — zero console errors on any of the three; the page/popup selector lists all 8 pages + the new "Event Available" popup for every event; rendered correctly at real mobile (390×844)/tablet (768×1024)/desktop (1920×1080) viewport sizes; the Candy Bowl was screenshotted at all 8 progress boundaries via the dev-preview's own simulator controls and visually confirmed to change states exactly where the automated proof says it should (bowl visibly empty at 24%, visibly holding a few candies at 25%, full at 100%); January's real effect canvases were confirmed present (3 canvases) at `performanceTier: "high"` and correctly absent (0 canvases) at `"low"`, and the page still rendered cleanly with `reducedMotion: "on"` simultaneously. **Incompatible/corrupt fallback, re-confirmed against the current build**: FDraft-Studio's own generic `sample-event.fdtheme`-shaped fixture (needs `opt-in-button` + `animations`, neither ever implemented) is still correctly, safely rejected with zero console errors; a non-`.fdtheme` real file path is still correctly, safely rejected as "could not be loaded." **Delete-original-sources, re-confirmed for both events with real imported assets** (`scripts/verify-delete-sources.ts`, generalised to take an event slug): Halloween and Christmas both independently proved — real assets copied to a scratch dir, project built from the copy, copy deleted entirely, `.fdstudio` reopened and recompiled successfully (8/8 assets survived for each). January has no image assets to delete-test (its art is procedural effects/tokens). `pnpm -r typecheck`, full studio suite (**443 tests**, up from 432), `pnpm run lint`, `pnpm run check:boundaries`/`check:architecture`, `pnpm --filter @fdraft/theme-sdk check:schemas`, and both `packages/*`'s own suites (168 + 171) all pass clean. **Remaining, honestly recorded as cosmetic, not blocking**: Halloween's 3 pre-existing `DECORATIVE_LAYER_COVERS_COMPONENT` design warnings (decorative images over the default template's nav bar, unchanged from row 13, still non-blocking); January's grey palette and "F* You, It's January" title still visually clip against the dev-preview's fixed-aspect-ratio box at narrow widths, a presentation detail of the dev tool's own box sizing, not the theme; Christmas's artwork remains FDraft's own "placeholder-quality... scaffold only" set (unchanged, not claimed as final); January's "scattered rubbish" placeholder remains unfilled (no real art exists, none invented) — both explicitly preserved, not decisions this row made. |

| 16. Built-in tutorial + Windows release-candidate preparation | FDraft-Studio only | Complete — tutorial shipped and tested; release-candidate source state prepared; no tag/installer/Release created (explicitly out of this row's scope) | **Pre-flight inspection, confirmed before any change**: working tree was the exact `b4fd8cb` commit (row 15's redo-dogfooding pass) with nothing unexplained; `apps/studio`/`src-tauri/tauri.conf.json`/`src-tauri/Cargo.toml` versions all already `0.1.0`, consistent; `@fdraft/theme-sdk`/`@fdraft/theme-renderer` both `0.1.0` (the released `theme-runtime-v0.1.0`); project-format `1.0.0` (min `0.9.0`), theme-format `1.0.0` (min `1.0.0`); `pnpm-lock.yaml` has no `file:`/`link:` override outside the workspace and no floating/`latest` pin; one existing tag/Release (`theme-runtime-v0.1.0`), no Studio tag/Release exists yet. Minimum compatible FDraft: the real `feature/fdraft-theme-runtime` branch at commit `bed5426` (FDraft's own `package.json` version `1.2.0-beta.9` — FDraft has no per-integration version tag of its own; the commit SHA is the precise, real pin until one exists). **No shared package, theme schema, or renderer contract change was made or needed** — this row is FDraft-Studio-only, confirmed by `git status` in the linked `../FDraft` checkout staying untouched throughout. <br><br>**Built-in tutorial** (`Help` button — always present on the Startup Screen and inside an open project, opens `Help → Tutorial`): 18 real sections (a welcome step, the 15 required topics, a dedicated "What FDraft Studio Cannot Change" section, and a finish step), every label cross-checked against the actual current UI source at the time of writing (`TopBar.tsx`, `AssetWorkspace.tsx`'s real "Export project…"/"Export theme…"/"Find unused" button text, `StartupScreen.tsx`'s real "Open Project…"/"Open Unpacked Project Folder…" labels) — never a mock-up or a planned feature. `Start Tutorial`/`Skip for Now`/`Back`/`Next`/`Finish`/`Close Tutorial`/`Restart Tutorial` all present and real. An optional first-run welcome offer shows exactly once (persisted via a new `apps/studio/src/tutorial/tutorialState.ts`, mirroring `recentProjects.ts`'s own exact read/write/atomic-write pattern in `appConfigDir` — no new persistence mechanism invented, `@tauri-apps/plugin-store` stays genuinely unused as before). The tutorial is mounted once at `App.tsx`'s `AppShell` level (above both `StudioShell` and `StartupScreen`, via a new `TutorialProvider`) so it is reachable with no project open and never touches project state — proven by a dedicated test asserting the open project's `dirty` flag and name are unchanged across a full tutorial open→close cycle. Bundled offline content: `USER_GUIDE.md`/`TROUBLESHOOTING.md` are copied verbatim into `apps/studio/src/tutorial/content/` and imported via Vite's native `?raw` (zero new dependency, zero network fetch — chosen over importing the top-level `docs/` directly across the app's own root, which risked an unproven dev-server `fs.allow` dependency) and rendered through a small, dependency-free Markdown-to-JSX pass (`renderMarkdown.tsx`) handling exactly the heading/paragraph/bullet-list shapes those two files use; a new `bundledDocsSync.test.ts` fails the build if the bundled copies ever drift from the canonical `docs/guides/` originals. **A genuine bug found and fixed by writing the tests, not by inspection**: the first implementation kept the tutorial's step/dialog state in a component that itself conditionally returned `null`, so `useModalA11y`'s real focus-on-mount effect fired once, before the modal's own ref ever attached to anything, and never again on subsequent opens — focus and keyboard-Tab-order were silently broken from the very first reopen. Fixed by splitting into an always-mounted thin wrapper plus a genuinely conditionally-*mounted* inner dialog (`{isOpen && <TutorialDialog />}`), matching the exact pattern every other Studio modal already uses — verified by a real focus-lands-inside-the-dialog test and a real Tab-order test, both of which caught the original bug immediately. <br><br>**Tutorial test coverage** (`apps/studio/test/tutorial/`, `apps/studio/test/ui/TutorialPanel.test.tsx` — 24 new tests across four files: 17 in `TutorialPanel.test.tsx`, 4 in `tutorialState.test.ts`, 2 in `bundledDocsSync.test.ts`, 1 in `tutorialMemoryLeak.test.tsx`): opening from Help, optional first-run presentation (and NOT re-offering once already shown), Skip for Now (records shown, not completed), Back/Next between real steps with Back correctly disabled on the first step, reaching Finish (persists completion), Restart Tutorial, closing and reopening (always restarts at the splash, never stuck mid-step), Escape-to-close (keyboard-only), a real focus-lands-inside-the-dialog assertion, Tab-then-Enter keyboard-only activation, a fake-timers proof that nothing auto-advances on its own (so reduced-motion has nothing to suppress — the whole tutorial is 100% user-paced by design), rendering without throwing at a 320px window width, project-dirty-state/name preservation across a full open-close cycle, offline availability (a `fetch` spy across a full run-through including opening both bundled docs, never called), missing/malformed bundled-content resilience (`renderMarkdown` given empty/whitespace-only/plain-text/malformed-heading input, asserted never to throw), and the canonical-docs sync check. A real, deterministic (never wall-clock-based, matching this project's own established aversion to flaky timing assertions) **memory-leak check** (`tutorialMemoryLeak.test.tsx`) opens and closes the dialog 25 times past a warm-up cycle and asserts `useModalA11y`'s own `keydown` listener is added and removed exactly once per cycle, with zero net accumulation. Existing tests needed one small, correct fix: `test/helpers/renderApp.tsx`'s `makeAppContext`/`renderWithApp` now also wraps `<TutorialProvider>` (since `StudioShell`/`StartupScreen` both call `useTutorial()` for their real Help button) and seeds tutorial state as "already shown" by default so a fresh test fixture is never mistaken for a genuine first launch — an explicit `{ freshTutorial: true }` opts a test back into the real first-run behaviour. <br><br>**Full release verification matrix, run clean**: `pnpm -r typecheck`; `pnpm run lint` (ESLint is this workspace's only style/format gate — there is no separate Prettier/formatter installed, an honest pre-existing fact, not something invented for this row); `pnpm run check:architecture`; `pnpm run check:boundaries`; `pnpm --filter @fdraft/theme-sdk check:schemas`; SDK tests (168), renderer tests (171, including the existing renderer-parity/fixture-compile-vs-fresh-compile test), Studio tests (**467**, up from 443 — exactly the 24 new tutorial tests); the official-event suite (`simulationCoverage.test.tsx`, 14 tests — all three events still compile, publish against a synthetic real-shaped FDraft fixture, and render, per row 15's own "a blocked publish is a test failure" rule); archive/SVG/security tests (Prompt 12's own, unaffected, still passing); save/recovery tests (unaffected); accessibility tests (`useModalA11y.test.tsx` plus the new tutorial keyboard/focus tests); Windows-path tests (`pathUtils.test.ts`, unaffected); a realistic-scale performance test (`packages/theme-sdk/test/performance/scale.test.ts`, unaffected); and the new tutorial memory-leak check. **Deliberately not done, per this row's own explicit scope**: no Git tag, GitHub Release, or installer build; no redesign of the editor, theme schema, or renderer contract; no change to FDraft (confirmed via `git status` in the linked checkout before and after); the Windows installer workflow (`release-studio-windows.yml`) remains untriggered — that is the one real, already-documented remaining blocker before an actual Windows release (see "Current blockers" below), unaffected by this row. |
| 17. Real Windows installer built, verified, and release plumbing hardened | FDraft-Studio only | Complete for everything a machine can prove; **publication deliberately not performed** — one gate genuinely cannot be satisfied from this environment (see the end of this row) | **The headline: a real Windows installer now exists and has been verified on real Windows, not reasoned about.** `FDraft-Studio-0.1.0-Windows-x64-Setup.exe`, 2,364,401 bytes, SHA-256 `5dbcf4b7c301d6fc56d6221a07b97976ad38030d6880c5cd4720504011c17420`, built on a `windows-latest` (Windows Server 2022, x64) runner from commit `dfc893e` by workflow run [33786939789](https://github.com/Burrichen/FDraft-Studio/actions/runs/33786939789). The checksum was re-verified **after downloading the artifact**, so it describes the bytes actually distributed, not just the build machine's copy. Committed verbatim as evidence in `docs/releases/candidates/` (manifest, checksum, smoke results — the installer binary itself deliberately not committed; it is a build output). Row 16's own "the workflow remains untriggered — that is the one real remaining blocker" is now closed. <br><br>**How, given the no-tag/no-Release constraint**: the only pre-existing mechanism (`release-studio-windows.yml`) was tag-triggered *and* created a GitHub Release via `tauri-apps/tauri-action`, so it could not be used to merely *verify* a build. A separate `workflow_dispatch`-only workflow was added that builds and uploads a plain, auto-expiring Actions artifact and never touches tags or Releases. Once Prompt 16 authorised release plumbing, that temporary verifier was folded back into a single canonical `release-studio-windows.yml` (below) rather than left as a second overlapping path. <br><br>**Six real, previously-unknown bugs that only a genuine Windows run could find** — every failed run was a real defect, not CI flakiness, and each is fixed: **(1)** No `.gitattributes` existed, so `windows-latest`'s default `core.autocrlf=true` rewrote every committed text fixture to CRLF on checkout, breaking content-hash manifest verification (`project.json` declared 10,476 bytes, the checked-out file was 10,857 — exactly +381, matching its 381 lines). This was never CI-only: Git for Windows' own installer defaults `core.autocrlf=true`, so any Windows contributor would have hit the identical corruption. Fixed with a root `.gitattributes` (`* text=auto eol=lf`, plus explicit `binary` for images/fonts/`.fdstudio`/`.fdtheme`/`.zip`, since those formats' integrity depends on exact bytes). **(2)** `cli.test.ts`'s `spawnSync("npx", …)` silently launched nothing on Windows — a `.cmd` shim is not directly executable by `CreateProcess` — surfacing as empty stdout/stderr and misleading exit codes rather than a spawn error; fixed with `shell: true` on win32 only. **(3)** `pathUtils.ts`'s `detectSeparator` only recognised Windows style from a drive letter or an existing backslash, so a bare relative fragment with no separator at all (`THEME_PROJECTS_DIR = "theme-projects"`, used for the publish diff's displayed paths and its `git add` hint) always fell back to a hardcoded `/` **regardless of host OS**. Fixed to fall back to the real running host's separator (`process.platform` in Node/Vitest, `navigator.userAgent` in a real Tauri webview, where `process` genuinely does not exist). **(3b)** That first fix then over-reached and broke the pre-existing, deliberate "pathUtils (POSIX-style paths)" contract — a path with an unambiguous leading `/` must keep its own style regardless of host (a project saved on macOS, later opened on Windows). Corrected so drive-letter/UNC/backslash means Windows, a leading `/` means POSIX, and only a genuinely ambiguous bare fragment consults the host; all three cases verified together against a simulated `win32` `process.platform`. **(4)** `simulationCoverage.test.tsx` and `verify-delete-sources.ts` spawned the literal path `node_modules/.bin/tsx`, the POSIX-only extensionless shim, failing with `ENOENT` on Windows; fixed by resolving tsx's real `dist/cli.mjs` through `require.resolve("tsx/package.json")` and running it via `process.execPath` — portable, shell-free, and immune to cmd.exe quoting for paths containing spaces. **(5)** Two real test races that a differently-scheduled runner exposed and macOS never did: `TutorialPanel.test.tsx` read persisted tutorial state immediately after a click without waiting for `TutorialContext`'s deliberate fire-and-forget write to land, and asserted on `document.activeElement` immediately after the dialog element appeared, before `useModalA11y`'s separate focus-on-mount effect had committed — both now polled via `waitFor`, matching this suite's own no-wall-clock convention. `tempDir.ts` also gained Node's documented `maxRetries`/`retryDelay` mitigation for a real `ENOTEMPTY`/`EBUSY` race in recursive cleanup. <br><br>**(6) The most significant finding, and it was not a Windows bug at all: ordinary Linux CI had been red on every commit since `b4fd8cb`, unnoticed.** `simulationCoverage.test.tsx`'s own header comment claimed hermeticity ("never depends on or mutates a real sibling repository") and that was true for the *publish* side (a synthetic fixture repo) — but never for the *asset* side: `build-halloween.ts`/`build-christmas.ts` default `ASSET_DIR` to the real sibling `../FDraft/public/events/<slug>/` when no 4th argv is passed, and the test never passed one. So it only ever worked on a machine that happens to have that checkout, and failed with `ENOENT` on every CI runner. It went unnoticed because prior phases verified locally and did not check the Actions result after pushing. Fixed with committed synthetic fixture PNGs (genuine, valid 1×1 files at the exact relative paths the scripts expect) passed explicitly as `ASSET_DIR`; real official-project building still defaults to the real approved artwork. **`ci.yml` is green on `main` again**, confirmed across two consecutive runs. A standing lesson recorded here deliberately: a local pass is not evidence that CI passes. <br><br>**Machine-verified packaged-application behaviour** (`windows-latest`, all green): silent install (`/S`) exits 0 with **no elevation prompt** (NSIS `installMode: "currentUser"`); the uninstall registry entry is created with the correct `DisplayVersion` (`0.1.0`) and `Publisher` ("FDraft Studio", **not** FDraft); `InstallLocation` is reported; `fdraft-studio.exe` exists there; a Start Menu shortcut is created; **the installed application actually launches and stays running** with no immediate crash; silent uninstall exits 0; and the registry entry, executable and shortcut are all removed. Uninstall preserves user data — and rather than trust structure alone, this project's pinned `@tauri-apps/cli` (2.11.4) `NsisConfig` schema was read directly from its own `config.schema.json` and confirmed to have **no delete-app-data option at all**, so nothing could enable it even by mistake. The release workflow additionally now tests the **upgrade/repair path** (re-running the installer over an existing install: exits 0, leaves exactly one uninstall entry and one Start Menu shortcut, keeps the executable, and preserves a real probe file), and writes real probe files into app-data so "user data survives uninstall" is proven against actual files rather than a directory the app may never have created. One honest nuance found while writing this: a passive 10-second smoke launch never writes app-data at all (nothing on the Rust side creates it eagerly; the frontend only does on real user action), so that condition is *recorded* rather than asserted, avoiding a check that would fail for a reason unrelated to the requirement. <br><br>**Windows identity, confirmed distinct from FDraft at every layer**: `productName` "FDraft Studio" vs "FDraft (Beta)"; `identifier` `com.fdraftstudio.app` vs `com.burrichen.fdraft` (so Windows derives entirely separate app-data/config/cache directories with no further configuration); executable `fdraft-studio.exe` vs `fdraft.exe`; separate uninstall entries. Studio's icon was confirmed by SHA-256 **not** to be a copy of FDraft's, and its `.ico` verified by parsing the file's own directory to contain six genuine resolutions (16/24/32/48/64/256 px at 32bpp) — reused by Tauri's bundler for the installer, executable and uninstaller. `bundle.targets` was narrowed from `"all"` to `["nsis"]`: **no `.msi` is produced**, because a second installer format that nothing has verified is worse than one that is. **File associations for `.fdstudio`/`.fdtheme` were deliberately omitted** — no such handler is implemented in the application, and the requirement was explicit that partial associations should be left out rather than shipped half-working. <br><br>**Hardened release workflow** (Prompt 16), replacing the `tauri-action` version and absorbing the temporary verifier so there is exactly one publish path: three jobs, **least-privilege by construction** — the workflow defaults to `contents: read`, `verify` and `build-windows` are read-only, and **only** `publish` takes `contents: write`. `publish` is further gated on `startsWith(github.ref, 'refs/tags/')`, so the `workflow_dispatch` path can build and smoke-test a candidate but **can never publish**. Every action is pinned to a **commit SHA** (deliberately the v4 line already proven to work here, not an unverified jump to newer majors); dependencies install from the committed lockfile with `--frozen-lockfile`; the build **fails** if the pushed tag, `apps/studio/package.json`, `src-tauri/tauri.conf.json` and `src-tauri/Cargo.toml` ever disagree on the version. Before publishing it re-verifies the downloaded artifact's SHA-256 **and** that `release-manifest.json` names that exact artifact, tag and commit — proving the published bytes are the tested bytes, not merely another build of the same commit — and refuses outright to overwrite an existing Release. No signing secret is referenced anywhere, because none exists. Prompt 16's required test categories are each run as their own **named** step (schema+migration 57, package round-trip 21, security+archive/SVG safety 35, renderer parity 3, tutorial 24, official events 14, Windows-path+save/recovery 37, accessibility+performance 12) so every category's result is explicit in the log rather than an unverifiable claim about a bundled suite; every one of those commands was executed locally first to confirm it actually resolves. <br><br>**A real discrepancy surfaced rather than papered over**: row 16 recorded the minimum compatible FDraft as commit `bed5426`, while this phase's first manifest said `006035c`. Read-only inspection of the sibling checkout settled it — both are right about different things. `006035c` is the true **minimum** (the first commit supporting all 14 keys plus `behaviour`/`effects`); `bed5426` is the **last commit actually verified against** (current `feature/fdraft-theme-runtime` HEAD, where the events were published). FDraft reports version `1.2.0-beta.9` at *both*, which is precisely why the SHA is the real pin. The manifest now emits `minCompatibleFdraftCommit`, `minCompatibleFdraftVersion`, `lastVerifiedFdraftCommit` and `testedFdraftPlatform` as separate fields so the distinction cannot collapse again. Also recorded plainly, because it materially qualifies any compatibility claim: **that FDraft branch is unmerged**, so theme support reaches ordinary FDraft users only once it lands. <br><br>**Documentation**: `docs/releases/STUDIO_RELEASE_NOTES.md` (the file the workflow publishes as release notes) and a new `docs/guides/WINDOWS_INSTALL.md` cover requirements, which installer to download, checksum verification, truthful signing/SmartScreen expectations, first launch, the tutorial, where projects are stored, backup, update and uninstall behaviour, diagnostics, and known limitations. Both state plainly that Studio is **Windows-only**, that the installer is **unsigned**, that there is **no automatic updater**, and that users should **never** disable Windows Security — and neither describes macOS/Linux builds or unimplemented features as available. A README **Download** section points at the Releases page as canonical. Two claims were fact-checked and corrected before shipping rather than guessed: the tutorial's step count (confirmed 18 in source) and WebView2's install behaviour (confirmed `downloadBootstrapper`, `silent: true`, from the pinned CLI's own schema); a desktop-shortcut claim and a "version shown in the tutorial" claim were **removed** because neither could be verified — the installer's UI strings are LZMA-compressed and unreadable, and no interactive Windows session exists. <br><br>**Publication deliberately not performed, and exactly why.** Every machine-checkable gate passes. What remains is a gate no CI runner or macOS workstation can satisfy: the interactive clean-Windows pass — walking the tutorial in the installed application, creating a project, opening each official starter project, importing an image, direct text editing, Copy Workspace, pop-up editing, save/reopen, recovery, responsive preview, simulation, behaviours, effects, `.fdstudio` export/import, `.fdtheme` compilation, and rendering a real exported theme **in a compatible Windows FDraft build** (Halloween, Christmas, January) including incompatible/corrupt-theme rejection and last-known-good fallback. **No interactive Windows environment exists here, and no Windows FDraft build exists at all** — FDraft's own release workflow has never been run — so `testedFdraftVersion`/`testedFdraftCommit`/`testedFdraftPlatform` are `null` and were not filled in. A second, independent blocker for a **public** release: **this repository declares no licence anywhere** — no `LICENSE` file and no `license` field in any `package.json` — so downloaders would receive no granted rights and the required "licence and third-party notices" cannot be attached, because none exist. Choosing a licence is the owner's decision, not something a release process should invent. Both gates are recorded as unchecked items in `RELEASE_CHECKLIST.md` section G, which also documents the exact publish procedure so that publishing is a single tag push once they are cleared. No tag was created, no GitHub Release was created, and nothing in the sibling FDraft repository was modified (confirmed by `git status` there before and after — it remains at `bed5426` with a clean tree). |

## Current blockers

- **The two gates that stop a public Windows release today** (see row 17 for full context;
  both are recorded as unchecked items in `docs/guides/RELEASE_CHECKLIST.md` section G):
  **(a)** No interactive clean-Windows verification pass has been performed, and no Windows
  FDraft build exists to test an exported theme against — FDraft's own release workflow has
  never been run. Everything a machine can check is green (full matrix on Windows, real
  installer, install/launch/uninstall/reinstall, checksums), but the human GUI pass and
  Windows FDraft interop cannot be done from a headless runner or a macOS workstation, so
  `testedFdraftVersion`/`testedFdraftCommit` remain `null` rather than guessed at.
  **(b)** This repository declares **no licence at all** — no `LICENSE` file, no `license`
  field in any `package.json`. For a public release that means downloaders receive no
  granted rights and the expected "licence and third-party notices" cannot be attached
  because none exist. Choosing a licence is the owner's call, not a release process's.
- **FDraft's theme-runtime branch is unmerged.** `feature/fdraft-theme-runtime` (@ `bed5426`)
  is where all theme support lives; until it lands on FDraft's default branch, no ordinary
  FDraft user can load a Studio theme regardless of what Studio ships.
- **Superseded:** Prompt 10 is done — see row 10 above. FDraft's sibling checkout is now on
  `feature/fdraft-theme-runtime` (base `main` @ `8bc5af7`, per that branch's own rollback
  section), committed and pushed. `docs/architecture/RENDERER_HOST_NOTES.md`'s candidate
  mappings are no longer the live reference — see `../FDraft/docs/fdraft-theme-runtime/
  INTEGRATION.md` instead. **Superseded again by row 14, now resolved**: FDraft's real
  supported-key/-capability counts moved from Prompt 10's original 7 keys/3 capabilities to
  all 14 default-template keys and 5 capabilities (`responsive`/`masters`/`popups`/
  `behaviour`/`effects`) — see row 14 for the full investigation and FDraft's commit `006035c`
  for the real implementation/test evidence. **Superseded again by row 15, now fully
  resolved**: Halloween, Christmas, and January are all now genuinely published into FDraft
  (`theme-projects/{halloween,christmas,f-you-it-s-january}/` and
  `src/theme-packs/{halloween,christmas,f-you-it-s-january}/`) and render correctly against
  the real dev server — see row 15 for the full redo-dogfooding evidence.
- **Resolved by Prompt 13** (row 13): real end-to-end verification of the dev-preview and
  publish workflows against the actual live `../FDraft` checkout has now been performed — a
  real Christmas publish landed in `../FDraft/theme-projects/christmas/`/
  `src/theme-packs/christmas/theme.fdtheme`, and FDraft's real `/theme-preview` dev route was
  driven with Playwright against a real running `next dev` server for all three starter
  events (Christmas renders; Halloween/January show FDraft's real incompatible-theme message
  with zero console errors).
- The Windows Studio installer workflow (`release-studio-windows.yml`) has never actually been
  triggered — needs a real `studio-v<version>` tag push (explicit sign-off required) and a real
  Windows machine to verify the resulting `.exe` actually installs/runs. This is the one
  remaining release-readiness blocker from Prompt 12 — see row 12.
- The renderer's reserved gaps (animation/effect playback, font loading, crop zoom,
  device-pixel-constant anchors, nested relative coordinates) are listed in
  `packages/theme-renderer/README.md` and expected to close in later prompts (9, 5-7).
- `apps/studio/src-tauri/Cargo.lock` is generated but not yet committed (only committed
  when the user asks for a commit).
- Prompt 5's Copy Workspace (project-wide grouped/searchable copy editing, placeholder
  insertion, reset/duplicate/where-used/safe-replace, overflow/contrast/unresolved-
  placeholder warnings), full smart-guide rendering, rich-text model, asset-based/rounded
  masks, and layer accessibility/description fields are all deferred — see the Phase 5 row
  above for the specific reasons each was scoped out rather than silently dropped.
- Guides and safe areas have no home in `@fdraft/theme-sdk`'s schema yet — persisting
  user-created guides or configuring visible safe areas needs a small schema addition,
  which was judged too significant to make unilaterally inside Prompt 5's scope; needs an
  explicit decision before that work can start.
- Prompt 6's native OS drag-and-drop import and clipboard-paste import are implemented but
  **not verified against the real Tauri desktop app** — only against the browser dev
  harness, consistent with this project's established screen-capture-safety policy (see
  memory: never attempt a blind/ambiguous-target OS screenshot). `dragDropEnabled: false`
  is now set in `tauri.conf.json`; the next person to run `pnpm tauri dev` should drag a
  real file onto the Asset Workspace and confirm it imports before this is treated as
  fully proven.
- Non-destructive image optimisation (`apps/studio/src/assets/imageOps.ts`) has no UI —
  the re-encode function exists and composes with `buildReplaceAssetSourceCommand`, but
  nobody can reach it from the Asset Workspace yet.
- Prompt 7's responsive-design bullets are the largest deliberate scope-down of the phase:
  no true nine-point anchor *stretch*, fixed/fill/hug sizing, 4-sided margins, min/max
  width/height constraints, proportional scaling across breakpoints, a preview-profile
  switcher (Desktop/Laptop/Small Window/Custom), or a responsive inspection mode exist yet —
  only single-edge/center anchoring and one-active-breakpoint transform overrides
  (pre-dating this phase) are implemented. See the Phase 7 row above for the full list.
- `ZoneKind` (header/sidebar/main/overlay/footer) is a logical classification tag on
  `ComponentLayer` only — there is no visual, resizable zone-region editor yet. Themes
  still can't be checked for "did this land in a compatible zone" except via the
  Validation panel's `INCOMPATIBLE_ZONE` warning.
- Border/gradient/shadow token picker UI in the Properties panel is deferred — the schema
  fields (`fillGradientTokenId`, `shadowTokenIds`, border tokens) and renderer wiring exist
  and are tested, and `apps/studio/src/editor/tokenCommands.ts` can create all five token
  types, but only Fill-color and Corner-radius got quick-create UI wired into
  `PropertiesPanel.tsx` this phase (border/gradient/shadow each need an existing
  `colorTokenId` dependency and a good compound quick-create UX was judged out of budget).
- Prompt 8's Behaviour Mode has no dedicated per-layer Interaction States editor in the
  Properties panel — `layer.interactionStates`, the evaluator, and the real hover/focus/
  pressed/selected DOM wiring all exist and are tested, but Studio's UI only reaches this
  through project-wide Behaviour rules (`BehaviourWorkspace`), a different, more general
  authoring surface than "select one layer, add a hover state" would be.
- `startAnimation`/`stopAnimation` Behaviour actions (`whileTrue` rules) now have a full
  visual effect as of Prompt 9 — `restartAnimation` and every other imperative action still
  only resolve as data pending the live click-dispatch work below.
- Live click-driven popup-open/close, page-navigation, and `restartAnimation` dispatch
  inside Studio's own preview is not wired — `resolveTriggeredRule` correctly determines the
  winning rule for a click/page-lifecycle edge event and is unit-tested, but nothing in
  Studio's preview calls it on a real click and performs the resulting on-screen navigation/
  popup-open/animation-restart yet. Continuous (`whileTrue`) rules, including real hover/
  focus/pressed/selected and now animation start/stop, **are** fully live in both the
  Behaviour Mode simulator and Design/Preview mode today.
- Prompt 9's Studio Animations section (Properties panel) only offers the 10 built-in
  presets — the schema, evaluator, and CSS-keyframe generation all fully support up to 12
  hand-authored custom keyframes (opacity/x/y/rotation/scale per step) and are tested
  directly against the renderer, but no UI exists yet to author a custom keyframe list, let
  alone a visual/drag-based timeline editor.
- No automated wall-clock performance-timing budget assertions exist for animations/effects
  (deliberately — timing-based test assertions are flaky on shared CI runners). The actual
  enforced budget is structural: fixed, documented per-tier particle/effect-layer/DPR caps
  (`PERFORMANCE_TIER_CAPS`), verified with deterministic unit tests instead.
- High-DPI canvas scaling reads and caps `devicePixelRatio` and is unit-tested, but has not
  been verified against a real physical high-DPI display — only a headless Chromium run at
  device-pixel-ratio 1, consistent with this project's established screenshot-safety
  caution around anything not literally checked on real hardware.

