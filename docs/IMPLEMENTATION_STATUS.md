# Implementation status

Update this file only with evidence. Keep commands, results, commit/tag references, and unresolved blockers concise.

| Phase | Repository | Status | Acceptance evidence |
| --- | --- | --- | --- |
| 1. Repository foundation | FDraft-Studio | Verified | `node scripts/check-architecture.mjs` passes. Sibling `../FDraft` confirmed (read-only). Both repos confirmed public via unauthenticated GitHub API. Added missing `.gitignore` and `.vscode/` editor settings; untracked stray `.DS_Store`. See `docs/architecture/BASELINE_AUDIT.md`. |
| 2. Theme SDK and package releases | FDraft-Studio | Implemented, partially complete | `@fdraft/theme-sdk` (v0.1.0) built: full versioned data model (Zod schemas + generated JSON Schema in `schemas/`), `.fdstudio`/`.fdtheme` pack/unpack with deterministic hashing, migration registry, semantic + SVG validation, archive security limits, and a `fdraft-theme` CLI. `pnpm --filter @fdraft/theme-sdk typecheck` and `pnpm --filter @fdraft/theme-sdk test` both pass (59/59 tests, 11 files — grew during Prompt 3: added an optional `canvas` size the renderer needs, and a `DISALLOWED_STYLE_PROPERTY` semantic check that was missing — a real component-style-override could previously use any globally-safe property, not just ones a specific requirement allowed). `pnpm --filter @fdraft/theme-sdk build` produces `dist/`; compiled CLI smoke-tested directly with `node dist/cli.js`. `pnpm check:schemas` passes (no drift). **Prompt 3 also split the public entry points** (`@fdraft/theme-sdk` main / `./packaging` / `./node`) after discovering the original single barrel unconditionally pulled `node:crypto` into any browser bundle that imported anything from it, even pure validation functions — see `docs/architecture/COMPATIBILITY_AND_RELEASES.md` and the package README. Distribution route decided and CI/release workflows authored (`.github/workflows/ci.yml`, `.github/workflows/release-theme-sdk.yml`) — see `docs/architecture/COMPATIBILITY_AND_RELEASES.md`. **Not done:** no tag has been pushed and no release exists yet — cutting `theme-sdk-v0.1.0` is a deliberate action left for the repository owner, not performed here. FDraft has not been touched. |
| 3. Shared renderer and fixture lab | FDraft-Studio | Implemented, partially complete | `@fdraft/theme-renderer` (v0.1.0) built: DOM-based `ThemeRenderer` (React 19), master-inheritance resolution, all 7 layer types, percentage-based responsive layout with `cqw`-scaled text, declarative condition/interaction-state evaluation, a component-adapter registry with a 7-key deterministic sample set (`page-title`, `event-information`, `event-countdown`, `draft-controls`, `film-grid`, `event-progress`, `points-counter`), and per-layer/per-page error boundaries with a host-overridable fallback. `apps/renderer-lab` (Vite + React) proves it end to end: loads the real unpacked project fixture and the real compiled `fixtures/projects/sample-event.fdtheme` binary via a Node-side Vite plugin, runs the SDK's real validators live in the browser as a compatibility preflight, and lets a user switch page/popup, viewport, reduced motion, performance tier, and mock image-state per scenario, including 5 deliberately-invalid/unsupported-version scenarios. `pnpm --filter @fdraft/theme-renderer typecheck/test/build` all pass (56/56 tests, 13 files, including one integration test rendering the real fixture and one asserting `.fdtheme`/fresh-compile parity). `pnpm --filter @fdraft/renderer-lab typecheck` and `build` (Rollup/production) both pass. `pnpm run lint` and `pnpm run check:boundaries` (new script) both pass across the whole workspace. **Actually launched and visually inspected** both the dev server and the production preview build with a headless-Chromium driver (screenshots + `console --errors` checked) — this caught and led to fixing two real bugs: (1) the theme-sdk browser-crypto barrel issue above, and (2) an impure-render bug (child calling parent `setState` during render) that was causing React's "recovered from concurrent-rendering error" spam. Distribution route mirrors theme-sdk exactly (`.github/workflows/release-theme-renderer.yml`, tag `theme-renderer-v<version>`; `pnpm pack` confirmed to rewrite the `workspace:*` dependency on `@fdraft/theme-sdk` to its exact resolved version). **Not done:** no tag pushed, no release exists — same deliberate deferral as theme-sdk. FDraft was only inspected read-only (see `docs/architecture/RENDERER_HOST_NOTES.md`); no FDraft file changed. **Reserved gaps** (see the package README for the full list): no real animation/effect playback, crop is position-only (no zoom), responsive anchors scale with canvas rather than staying device-pixel-constant, no `@font-face` loading, and nested layers don't get parent-relative coordinates. |
| 4. Studio application lifecycle | FDraft-Studio | Implemented, partially complete | `@fdraft/studio` (v0.1.0) built: a genuinely separate Tauri v2 + React 19 desktop app under `apps/studio` — distinct package, entry point, bundle id (`com.fdraftstudio.app`), window title ("FDraft Studio"), app-data/config locations, tests, and build output. **Actually launched** (`pnpm tauri dev`) as a real, separate OS process (confirmed via `ps`/System Events, not assumed) with the correct window title, and its real rendered UI content verified via the macOS accessibility tree (every StartupScreen button/heading/field present and correct) — screenshots were *not* used for this because the window kept opening on a different virtual desktop than whatever was visible, and two blind screen-capture attempts caught unrelated personal app content (Chrome, Slack) instead; both images were deleted immediately and further attempts were abandoned rather than risk a third. Also drove a real interaction end-to-end: clicking "Open Project…" opened a genuine native macOS file-picker sheet, proving the Tauri IPC bridge to Rust's dialog plugin actually works. `pnpm tauri build` produced real installers: `FDraft Studio.app` and `FDraft Studio_0.1.0_aarch64.dmg` (3.1 MB), bundle identifier and version confirmed via `plutil`. Project lifecycle (new/open/save/Save As/duplicate/import `.fdtheme`/export backup), atomic saves (temp-file-or-directory + validate + rename, with directory saves using a rename-aside backup so a crash mid-swap never loses the original), autosave-to-a-separate-recovery-slot with a next-launch recovery prompt, named snapshots with restore-as-new-version and bounded retention (20/project), command-based undo/redo with transaction grouping and bounded history (100 entries), and a recent-projects list with missing-path detection are all implemented in framework-agnostic TypeScript behind a `StudioPlatform` interface — real Tauri plugins in production, a real `node:fs`-backed test double in tests — so `pnpm --filter @fdraft/studio test` (60/60 tests, 11 files) exercises genuine filesystem semantics (real `rename`, real crash-mid-swap simulation) without needing the Rust toolchain. `pnpm --filter @fdraft/studio typecheck`/`build` and `cargo check`/`clippy --all-targets -- -D warnings`/`fmt --check` (from `src-tauri/`) all pass clean. The workspace shell (top command bar, collapsible Pages/Layers and Properties panels, central preview via `@fdraft/theme-renderer` with Studio's own mock component-adapter set, Design/Behaviour/Preview mode switch — Preview mode confirmed via RTL test to hide every editor control) renders the current page live; project metadata editing is wired through the real undo/redo stack. **Discovered and fixed while integrating** (not deferred): `@fdraft/theme-sdk`'s `sha256Hex` still used `node:crypto`, which is unusable in a Tauri webview exactly like the browser-bundle problem Prompt 3 hit — switched to async Web Crypto (`crypto.subtle`), making the entire `@fdraft/theme-sdk/packaging` entry point genuinely browser/webview-safe (not just the main entry point); `unpackFdtheme` validated a theme's assets but never actually returned their bytes, which a real theme-import feature needs — added an `assets` field to `FdthemeUnpackResult`. **Not done:** no direct-manipulation editor (layer selection/drag/property editing) — Design/Behaviour modes render read-only via the shared renderer, as directed. No per-OS installer CI matrix yet (local `tauri build` proved it works; CI runs `cargo check`/`clippy`/`fmt` + the frontend build/tests only). See `apps/studio/README.md` for exact filesystem locations and save-safety details. FDraft was not touched (confirmed via `git status` in `../FDraft` before and after this phase). |
| 5. Visual editor and layer tools | FDraft-Studio | Implemented, deliberately scoped core | Built a real, tested subset of the direct-manipulation editor rather than shallow coverage of every bullet — see "Deferred" below for the honest gap list. Pure, framework-free editor logic in `apps/studio/src/editor/` (`layerTree.ts`, `geometry.ts`, `selection.ts`, `viewState.ts`, `layerCommands.ts`, `containerRef.ts`, `clipboard.ts`) covers layer-tree traversal/reparenting with cycle prevention, rotation-aware bounding boxes and resize math (verified against a rotated-box case, not just the unrotated one), snap-line computation, pan/zoom/zoom-to-fit view math, and ~25 `Command<StudioProjectDocument>` factories (transform, opacity, crop, mask, z-order via sibling swap/front/back, lock/hide, rename, group/ungroup with cycle checks, duplicate/delete/paste with deep id-remapping, align, distribute, text edit/alignment). `ProjectSession` gained a public `applyCommand`/`beginTransaction`/`commitTransaction` API so every one of these composes with the existing undo/redo stack and dirty tracking. UI: `Canvas.tsx` renders an absolutely-positioned selection/handle overlay *on top of the real `ThemeRenderer` output* (keyed off its `data-fdraft-layer-id` markers, per the "no separate canvas implementation" requirement) with pan/zoom/zoom-to-fit/100%, single/shift-multi/marquee select, 8-handle resize + rotate handle (both rotation-aware), drag-move with page/other-object snapping, keyboard nudge (precise/large-step modifiers), copy/cut/paste/paste-in-place/duplicate/delete/select-all/group/ungroup/z-order shortcuts, and double-click inline text editing with a Properties-panel fallback. `LayersPanel.tsx` (nested tree, icons, visibility/lock toggles, drag-reorder with cycle prevention, search) and `PropertiesPanel.tsx` (X/Y/W/H/rotation/opacity, text/alignment, crop reset, mask type, arrange/align/distribute/group buttons) are wired into the existing shell's Left/Right panels. Added a distraction-free Focus mode (hides all chrome, canvas stays fully editable, Esc or a floating button exits) alongside the existing independently-collapsible panels and Preview mode. `pnpm --filter @fdraft/studio typecheck`, `lint`, and `test` all pass (16 files, 132/132 tests — up from 60 at the end of Prompt 4), including new `layerTree`/`geometry`/`selection`/`viewState`/`layerCommands`/`ProjectSession` transaction suites. Full workspace: `pnpm -r typecheck` and `pnpm -r test` pass (259 tests total across theme-sdk 59, theme-renderer 58, studio 132, renderer-lab has no test suite of its own). **Actually launched and interacted with** via a temporary in-memory-platform harness driven by headless Chromium against the real Vite dev server (never a blind OS-window screenshot, consistent with this project's established privacy-safe verification method) — this caught and fixed three real bugs before they shipped: (1) `GroupLayerView`'s full-stage `position:absolute;inset:0` wrapper had no `pointer-events:none`, so any group placed above other content in z-order silently swallowed clicks across the *entire* canvas, not just its own children — this is a `packages/theme-renderer` fix (with a regression test) that also matters for FDraft's real runtime, not just Studio's editor; (2) the drag/resize/rotate commit-on-gesture-end read React state through a stale closure and silently no-op'd every transform commit — fixed with a ref mirroring the draft state, verified by an actual before/after screenshot of a working drag; (3) missing `box-sizing: border-box` was clipping the Properties panel's W/H/Opacity fields at laptop-width windows — added a global reset. Verified visually at desktop (1440px), laptop (1100px), and small-window (760px) sizes; confirmed independent panel collapse, marquee multi-select (including a rotated group), undo/redo across a 3-step gesture chain (visibility → rotate → resize), and Focus mode entry/exit. `../FDraft` confirmed untouched via `git status` (clean, `feat/update1`) both before and after this phase. **Deferred, honestly incomplete:** full smart-guide visual rendering and true ruler-drag guide creation (guides only exist as a scoped-down concept, not yet implemented as session-local state — no persistence path exists without a `theme-sdk` schema addition for guides/safe-areas, which was not made unilaterally); the allowlisted rich-text model (plain text only); live contrast/overflow/truncation/unresolved-placeholder warnings and the Copy Workspace panel itself (text editing exists per-layer via Properties/double-click, but there is no project-wide grouped/searchable Copy Workspace view, placeholder-token insertion UI, or reset-to-default/where-used/safe-replace); asset-based and rounded-rect masks (only none/rect/ellipse are offered — the schema's `MaskType` has no distinct "rounded" variant, and asset-based needs an asset picker that doesn't exist yet); accessibility/description fields on layers (no such field exists in the `theme-sdk` schema — adding one was judged out of scope for a unilateral change to the shared contract); live accessibility announcements beyond basic `aria-label`/`aria-pressed`/keyboard equivalents; the Properties panel's numeric fields don't live-resync while a layer stays selected through an external change (e.g. a canvas drag) — correct after reselecting, a known rough edge from the deliberate no-effect-resync pattern used elsewhere in this codebase; parent-relative group coordinates (still a reserved renderer gap from Prompt 3, so Studio's group bounding box is informational/Studio-only, not the renderer's actual paint position, exactly as documented there). |
| 6. Asset Workspace and import/export | FDraft-Studio | Implemented, deliberately scoped core | Extended the shared data model additively (`@fdraft/theme-sdk`): `AssetRecord` gained `name`/`originalFileName`/`tags`/`folderId` (all optional, editor-only — `compileTheme` strips them before anything reaches a runtime theme), a new `AssetFolder` schema, and `StudioProjectDocument.assetFolders` — schema drift is clean (`pnpm check:schemas` passes) and no migration was needed (purely additive optional fields, same pattern as Prompt 3's `canvas` field). Added `sanitizeSvg` alongside the existing reject-only `checkSvgSafety` — strips `<script>`/`on*` handlers/`foreignObject`/external+`javascript:` URLs/`@import`/custom entities, then re-validates and refuses to call anything clean unless it actually is (15/15 SVG tests, up from 8). Exported `findAssetUsage`/`collectUsedAssetIds`/`detectCapabilities` from the main entry point and rewired `compileTheme` to use them internally, so Studio's usage-count/where-used/unused-asset UI and the compiler's own asset-pruning logic are provably the same rule, never two implementations that could drift (6 new `compile/assetUsage` tests). `apps/studio/src/assets/` (new): `fileNames.ts` (Unicode NFC, Windows-reserved-name/illegal-character/trailing-dot stripping, display-name dedup — 11 tests), `assetImport.ts` (extension/size validation, SVG sanitise-or-reject, content-hash dedup/reuse detection, all wired through the SDK's real `computeAssetPath`/`sha256Hex`/`MAX_FILE_SIZE_BYTES` — 11 tests), `assetCommands.ts` (23 `Command<StudioProjectDocument>` factories: add/rename/move/tag/replace-source-preserving-id/duplicate/delete, folders, image-state-group add/rename/set-default/add-state/reassign-state-asset/delete-state — 22 tests), `assetUsageSummary.ts` (usage counts, unused-asset detection, missing/corrupt health checks via real hash re-verification — 5 tests), `imageOps.ts` (browser-only dimension measurement and Canvas-based re-encode for "optimise", not unit-tested for the same reason `Canvas.tsx`'s DOM gestures aren't — verified visually instead). `apps/studio/src/project/`: `pruneAssetsToProject` (the asset byte pool is additive-only during a session — undoing a delete always finds its bytes again — and gets pruned back down to exactly what's referenced only at save/export time, which is also what makes a genuinely-missing asset surface as a real, loud `MISSING_ASSET` at save instead of a silent gap), `exportRuntimeTheme` (the `.fdtheme` export path that never existed before this phase — the SDK had `compileProjectToFdtheme` ready and nothing in Studio called it), `exportAnalysis.ts` (`analyzeThemeExport`/`analyzeProjectExport` — a real compiled/packed byte count, not an estimate, plus capabilities/required-components/asset-counts/warnings/blocking-errors, computed before any write happens). `ProjectSession` gained `mergeAssetBytes` and `exportRuntimeTheme`. New UI: `AssetWorkspace.tsx` (grid/list views, folders, tag/name search, drag-drop + clipboard-paste + native multi-file-dialog import with per-file error reporting, transparency-checkerboard thumbnails, unused-asset badges, missing/corrupt badges), `AssetDetailPanel.tsx` (name/tags/folder editing, format/dimensions/size/hash/usage facts, a real "where used" list resolving to page/popup/master and layer names, replace-source/duplicate/delete), `ImageStateGroupsPanel.tsx` (add/rename groups, add/remove/reassign states, star-toggle default — refuses to delete the last state or the current default), `ExportDialog.tsx` (the analysis above, shared by both `.fdstudio` and `.fdtheme` export, atomic write via the existing `atomicWriteFile` machinery). Wired in as a fourth `"assets"` shell mode alongside Design/Behaviour/Preview. Native multi-file selection (`StudioPlatform.openFiles`) added to the platform interface and its Tauri implementation; `tauri.conf.json` sets `dragDropEnabled: false` so the webview's standard HTML5 drop events (`DataTransfer.files`) reach the Asset Workspace's drop zone instead of being intercepted by Tauri's native (path-only) drag-drop, which needs no new IPC plumbing and works identically in the browser dev harness and the shipped app — **not verified against a real native OS drag gesture on the actual desktop app** (see Deferred). `pnpm --filter @fdraft/studio typecheck`/`lint`/`test` all pass (22 files, 201/201 tests, up from 132). Full workspace: `pnpm -r typecheck`, `pnpm -r test` (332 tests: theme-sdk 73, theme-renderer 58, studio 201), `pnpm run lint`, `pnpm run check:boundaries`, and `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. **Actually launched and interacted with** via the same temporary in-memory-platform-plus-headless-Chromium method as Prompt 5 (never a blind OS screenshot): imported real files through the grid, opened the detail panel and confirmed real "where used" resolution, expanded an image-state group and reassigned a state's asset, opened the Export Theme dialog and confirmed a *real* compiled package size (2.3 KB) with capabilities/warnings, ran Find Unused end-to-end (3→0, assets actually removed, undo restored them), and re-ran `apps/renderer-lab`'s dev server to confirm the Prompt-5-era baseline scenario still renders pixel-identical with zero console errors after the `theme-renderer` pointer-events fix. Verified visually at desktop/laptop/small-window widths — laptop reflows cleanly, small-window (760px) is usable but tight (no dedicated collapse toggle for the folders/detail panels the way Design mode has, unlike the rest of the shell — see Deferred). **Deferred, honestly incomplete:** real native OS drag-and-drop was never exercised against the actual Tauri desktop app (the code path is real and standard, `dragDropEnabled: false` plus HTML5 `DataTransfer.files`, but device-level verification carries the same risk this project's established screenshot-safety policy exists to avoid — see memory); clipboard-paste likewise relies on the standard web `paste`/`ClipboardEvent` API rather than a Tauri clipboard plugin (none is installed) and wasn't device-verified either; non-destructive image optimisation (`imageOps.reencodeImage`, Canvas-based PNG/JPEG/WebP re-encode at a chosen quality) has no UI wired to it yet — the function exists and is undoable via the same replace-source command, but there's no before/after size-comparison dialog; the Asset Workspace's folders/detail side panels don't independently collapse the way Design mode's do, so very small windows get cramped rather than reclaiming space; rounded-rect and asset-based masks are still not offered anywhere (unchanged from Prompt 5 — the schema's `MaskType` has no distinct "rounded" variant and asset-based masking still needs a picker UI); duplicate-*content* detection during import reuses the existing asset automatically rather than prompting the user to choose, which is a defensible default but not the only reasonable one. |
| 7. Pages, masters, components, responsive layout | FDraft-Studio | Implemented, deliberately scoped core | Built a real, tested core subset of this prompt's very large scope rather than shallow coverage of every bullet — see "Deferred" below for the honest gap list. **Project structure/navigation:** extended `@fdraft/theme-sdk` additively — `MasterLayerOverride` (`transform`-partial/`visible`/`opacity` only, keyed by the inherited master layer's own id, never a same-id shadow layer that would trip the existing `DUPLICATE_ID` check) on both `Page.masterLayerOverrides`/`Popup.masterLayerOverrides`; `ZoneKind` enum (`header`/`sidebar`/`main`/`overlay`/`footer`) as a logical classification tag on `ComponentLayer`; `ComponentRequirement` gained `singleton?`/`compatibleZoneKinds?`/`minWidthPx?`/`minHeightPx?`. `packages/theme-renderer`'s `resolveContainerLayers` now applies master-layer overrides (including recursively into nested groups) before concatenating a container's own layers — the single resolution path reused by both real rendering and Studio's "detach from master" (fresh-id deep clone of the resolved inherited layers). `apps/studio/src/project/containerCommands.ts` (new): full page/popup/master CRUD (add/rename/duplicate/reorder/delete), `findMasterDependents`/refuse-delete-with-dependents, `wouldCreateMasterCycle` (inheritance-cycle prevention), `setContainerMaster`/`setMasterLayerOverride`/`buildDetachFromMasterCommand` — 19/19 tests, all passing first try. `LeftPanel.tsx` rewritten for Add/Rename/Duplicate/Delete on pages/popups and Add/Rename/Delete (with a dependency-check confirm) on masters; new `MasterInheritancePanel.tsx` (master-assignment dropdown, inherited-layer list with override badges/visible-checkbox/reset, detach button); `Canvas.tsx` renders a master being edited directly through a synthetic throwaway page injection (`MASTER_PREVIEW_PAGE_ID` sentinel) since `ThemeRenderer` only targets a page/popup — reuses the real inheritance-resolution path rather than a second rendering implementation. **FDraft Default Event template:** `apps/studio/src/templates/fdraftEventContract.ts`/`fdraftEventTemplate.ts` build the guaranteed 8-surface project (Event Landing, Draft, Results, Completion, About/Information, Event Available, Join, Event Complete) directly from a registered page/component contract (not a visual guess or React-source conversion), with every author-written string given a stable editable copy slot and an approved FDraft default; its component/zone/singleton metadata was deliberately balanced so the template produces **zero** `checkDesignWarnings` — verified by a dedicated test, and visually confirmed (0 blocking errors, 0 warnings in the Validate panel). **Styling tokens:** `ShapeLayer` gained `fillGradientTokenId`/`cornerRadiusTokenId`/`shadowTokenIds`; `TextLayer` gained `fontWeightOverride`/`lineHeightMultiplier`/`letterSpacingPx`/`wrap`; `packages/theme-renderer/src/tokenStyle.ts` (new) resolves gradient/shadow-list/radius CSS from tokens, wired into `ShapeLayerView`/`TextLayerView` (one global token edit updates every linked use, by construction — a layer only stores a token id). `apps/studio/src/editor/tokenCommands.ts` (new) adds color/gradient/border/radius/shadow token-creation commands; `PropertiesPanel.tsx` gained a `ShapeLayerFields` section with Fill-color and Corner-radius dropdowns, each with an inline "+ New…" quick-create — verified end-to-end via a real UI test (`ShapeStyling.test.tsx`) that selects "+ New color…"/"+ New radius…" and asserts the token is created *and* assigned to the shape. **FDraft components and copy contract:** `packages/theme-renderer`'s sample adapter set expanded from 7 to 16 components (added event navigation, draft progress, film card, event points counter, profile badge, generate-draft action, complete/watch action, challenge card, results/completion content); a new, additive `ComponentCopyContractRegistry`/`ComponentCopySlotDeclaration` (default/fallback text, required/optional, allowed typed placeholders, character guidance, accessible-name fallback) sits alongside the existing adapter registry so no breaking change touched the 12 files referencing it; `resolveComponentCopy` substitutes host/sample placeholder values and falls back to declared defaults, refusing to let a required action label resolve empty. `ComponentLayerFields` in `PropertiesPanel.tsx` exposes a Zone dropdown plus one `CopySlotField` per declared slot (placeholder shows the FDraft default, a "Using FDraft default" hint, character-count guidance). **Validation panel:** `packages/theme-sdk/src/validation/designWarnings.ts` (new) is a deliberately separate, non-blocking advisory channel from `checkSemantics` — project-wide missing-required-component detection (a self-caught design bug: the first draft ran this per-container, which would have falsely flagged a template using a required component on only one of several pages — fixed and regression-tested), duplicate-singleton, incompatible-zone, undersized-component, WCAG contrast-ratio math for overlapping text-over-shape pairs, and an AABB-overlap+z-order heuristic for decorative layers covering protected components (16 tests). `ValidationPanel.tsx` (new) lists both channels, with "Go to" navigation for design warnings (whose path format is fully controlled) — plain text, no jump button, for `checkSemantics`'s Zod-derived paths, an honest scope limit rather than parsing two incompatible path grammars. **Starter templates:** `apps/studio/src/templates/starterTemplates.ts` (new) — Standard FDraft (= the FDraft Default Event template), Immersive, Minimal, Poster, Blank, all built through the exact same `StudioProjectDocument` schema/editor as any hand-built project, no hidden one-off rendering code (18 tests). `ProjectSession.newProjectFromTemplate(name, templateId)` added as a new method (existing `newProject(name)`'s single-blank-page default left unchanged, since dozens of pre-existing tests depend on it) and wired into a new `StartupScreen` template picker. `pnpm --filter @fdraft/studio typecheck`/`lint`/`test` all pass (30 files, 260/260 tests, up from 201 at the end of Prompt 6). Full workspace: `pnpm -r typecheck`, `pnpm -r test` (429 tests: theme-sdk 89, theme-renderer 80, studio 260), `pnpm run lint`, `pnpm run check:boundaries`, and `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. **Actually launched and interacted with** via the same temporary in-memory-platform-plus-headless-Chromium method as Prompts 5/6 (never a blind OS screenshot): opened the FDraft Default Event template in the dev harness and confirmed all 8 pages listed with correct icons, the Event Landing page rendering real component adapters (countdown, event-info card, profile badge, "Generate My Draft" action) with resolved copy text; opened the Validate panel and confirmed "Blocking errors (0)" / "Warnings (0)" matching the automated test; confirmed the rebuilt `StartupScreen` template picker shows the "Standard FDraft" default with its description text; re-ran `apps/renderer-lab`'s dev server and confirmed the default scenario still renders correctly after the sample-adapter/copy-contract expansion (16 components now, vs. 7 before — this app's own `App.tsx` needed a `copyContracts` prop added, a real regression caught and fixed during this phase), the "Invalid — duplicate ids" scenario still shows its red validation panel instead of crashing, and the mobile/desktop compare toggle still renders two viewport frames. Zero console errors across every screenshot. `../FDraft` confirmed untouched via `git status` (clean, `feat/update1`) both before and after this phase. **Deferred, honestly incomplete:** true nine-point anchor *stretch* (opposing anchors resizing width/height — only single-edge/center anchoring and one-active-breakpoint transform overrides exist, pre-dating this phase), fixed/fill/hug sizing modes, a 4-sided margin concept distinct from per-anchor offset, min/max width/height constraints, proportional scaling across breakpoints, a Desktop/Laptop/Small-Window/Custom preview-profile switcher UI, and a "responsive inspection mode comparing all sizes with direct navigation to warnings" — all real gaps confirmed by schema-level research, not attempted as a shallow/fake version; `ZoneKind` is a logical tag only, not a resizable/spatial UI region (no visual resizable header/sidebar/main/overlay/footer zone editor); border/gradient/shadow token picker UI in the Properties panel (the schema fields and renderer wiring exist and are tested, but each needs an existing `colorTokenId` dependency and a good compound quick-create UX was judged out of budget this phase — only Fill-color and Corner-radius got picker UI); rounded-rect and asset-based masks (unchanged from Prompts 5/6); full accessibility auditing beyond the AABB-overlap/contrast-ratio heuristics already built (no true hit-testing/DOM measurement, no keyboard-navigation-order check beyond "unreachable controls" via zone/z-order heuristics). Real FDraft component adapters remain unimplemented by design until Prompt 10. |
| 8. Behaviour Mode | FDraft-Studio | Implemented, deliberately scoped core | Built a real, tested no-code rule engine — one shared, deterministic evaluator used by both Studio's live preview and (per the prompt's explicit requirement) the same code path FDraft will eventually consume, plus a genuinely functional rule-builder UI — rather than shallow coverage of every bullet; see "Deferred" below. **Schema (`@fdraft/theme-sdk`, all additive):** `RuntimeVariable` — a closed 16-kind union covering exactly the prompt's read-only render context (event status/active/available, opted-in, current page/popup, draft generated, progress percentage, watched/target counts, event completed, performance tier, reduced motion, a per-layer `interactionFlag` for hover/focus/pressed/selected, an `imageState` variable, and a host-named `dateTime` value) — deliberately excludes unrestricted profile records, filesystem access, points mutation, draft generation, watch-state mutation, and eligibility decisions, exactly as directed. The existing `Condition` union (unchanged variants preserved) gained `compare` (`eq`/`neq`/`gt`/`gte`/`lt`/`lte`), `inRange` (inclusive), and `boolean` nodes. New `behaviour.ts`: `BehaviourTrigger` (the continuous `whileTrue` case, 8 lifecycle/interaction edge triggers — page enter/exit, popup open/close, click/hover/focus/blur — `eventPhaseChange`, and a general `conditionBecomesTrue` edge), `BehaviourAction` (`show`/`hide`/`setEnabled`/`setImageState`/`applyStyleOverride` — reusing the exact same closed style-property allowlist as the existing `ComponentStyleOverride` mechanism, never a new one — `startAnimation`/`stopAnimation`/`restartAnimation`/`openPopup`/`closePopup`/`navigateToPage`/`selectCopyVariant`), and `BehaviourRule` (id/name/enabled/priority/trigger/condition/actions). `ComponentLayer` gained `copyVariants` — named alternative wordings per copy slot, each with a stable id a rule's `selectCopyVariant` action selects by — kept structurally separate from `copyOverrides`/Behaviour expressions, exactly matching "copy may contain only copy-slot text... rules select a declared copy variant by stable ID." `StudioProjectDocument`/`RuntimeThemeDocument` gained `behaviourRules: BehaviourRule[]`; `ThemeCapability` gained `"behaviour"`. **Validation (`@fdraft/theme-sdk`):** new `checkBehaviourRules` (folded into `checkSemantics`) — impossible references (every trigger/action target: pages, popups, layers, image states, animations, component requirements, copy variants), type mismatches (a numeric operator on a boolean/string variable, a boolean check on a numeric variable, a compared value of the wrong type), unsafe actions (hiding/disabling a *required* component, a style property outside its requirement's allowlist), and an obvious self-trigger loop (a rule whose own condition reads the exact image-state group its own action writes) — 20 tests. `checkDesignWarnings` gained `HOVER_ONLY_DISCOVERY` (a layer only ever revealed by a hover-based interaction state, with no equivalent focus-based state — "never make hover the only way to discover essential information," made actionable). **The shared evaluator (`@fdraft/theme-renderer`)** — the same code Studio's preview and, later, FDraft both call, never a Studio-only interpretation: `readRuntimeVariable` (never throws on a missing/undisclosed value — every comparison simply reads false), extended `evaluateCondition`, `resolveActiveBehaviourRules` (resolves every `whileTrue` rule's effect on one render-state snapshot — rules never see each other's resulting overrides within a pass, so the evaluator is a single non-recursive walk that **cannot loop at runtime regardless of what a theme declares**; deterministic conflict resolution — higher `priority` wins, equal priority broken by declaration order with the later rule winning — and a full `BehaviourResolution.trace` recording *every* candidate rule per contested target, enabled or not, condition-true or not, and which one won: the "trace/debug view explaining which rule won and why"), `resolveTriggeredRule` (the same treatment for edge-triggered rules — a host dispatches the actual click/page-transition/condition-edge and is responsible for executing the winning rule's actions through its own host adapter; the renderer only ever returns data, never performs navigation/DOM mutation itself). Real hover/focus/pressed/selected support: `LayerFrame` wires native pointer/focus/click events to an optional `onInteractionFlagChange` host callback (never internal state — the renderer stays a pure function of `RenderState`, consistent with its existing architecture), with `applyInteractionFlagChange` as the exported pure-update helper a host folds into the `RenderState` it re-supplies. `ComponentLayerView` resolves an active `setEnabled`/`applyStyleOverride`/`selectCopyVariant` override on top of the layer's own static configuration; `ImageLayerView` resolves an active `setImageState` override ahead of the host-supplied active state. 44 new/changed tests in this package (80→124), including a dedicated **Candy Bowl fixture** (`test/fixtures/candyBowl.test.ts`) proving a three-state image-state group switches at configured, boundary-exact `progressPercent` ranges through the real evaluator — the ranges and state names are ordinary editable rule/project content, never hard-coded Halloween logic anywhere in the renderer. **Studio UI:** Behaviour Mode (previously a placeholder label) is now `BehaviourWorkspace.tsx` — a rule list (enable toggle, up/down reorder, duplicate, delete, readable one-sentence summaries via new `describeBehaviourRule.ts`), a fully recursive `ConditionEditor` (every `and`/`or`/`compare`/`inRange`/`boolean`/`stateEquals`/`eventPhase` node type, with "negate" as a checkbox at any depth rather than a separate tree shape), an `ActionEditor` (one row per declared action type, target pickers drawn from the real project — layers/pages/popups/animations/component requirements/copy variants), a live **Simulator** (event status/active/available/opted-in/draft-generated/completed toggles, progress/watched/target-count numbers, performance tier, reduced motion) driving a real `<ThemeRenderer>` preview, and a **Show trace** panel rendering `BehaviourResolution.trace` with real rule names via `describeTargetKey`. Hover/focus/pressed/selected are **not** manual simulator toggles — they come from actually interacting with the live preview (mouse/keyboard/click), reusing the exact `onInteractionFlagChange` wiring FDraft's real runtime would drive identically. New `behaviourCommands.ts` (11 `Command<StudioProjectDocument>` factories, all undoable). 23 new studio tests (260→283) including a real end-to-end test that fires a genuine `mouseEnter` DOM event on the live preview and asserts the trace panel flips from "condition false" to "condition true." **Two real bugs caught and fixed by these tests before shipping** (not deferred): (1) `resolveTriggeredRule` computed `conditionMet` from only the rule's own gating `condition`, never the `conditionBecomesTrue` trigger's own embedded condition — meaning a rule of that trigger type would fire unconditionally regardless of what it was supposed to be watching; caught by a dedicated unit test, fixed, regression-tested. (2) The initial `interactionFlag` variable design was purely ambient/contextual (meaning "whichever layer this condition happens to be attached to") — which works for a per-layer `InteractionState` but is meaningless for a project-wide `whileTrue` Behaviour rule, which has no such "current layer"; caught by the real-hover UI test silently never turning true, fixed by adding an optional explicit `layerId` directly on the variable (backward-compatible — ambient behaviour is preserved when it's omitted), with the broken-reference check, the `HOVER_ONLY_DISCOVERY` layer-scoping, and the readable-sentence generator all updated to match. **Cross-host contract fixture:** the canonical `fixtures/projects/sample-event` fixture (regenerated) now carries one real `BehaviourRule` — hovering the poster image now actually drives its own image-state group via `setImageState`, not just a static opacity tweak the way the pre-existing `stateEquals` `InteractionState` on the same layer only reacts to — verified through a real pack/compile/unpack round-trip, giving Prompt 10 a genuine Behaviour-rule example to integrate against. Full workspace: `pnpm -r typecheck`, `pnpm -r test` (532 tests: theme-sdk 125, theme-renderer 124, studio 283), `pnpm run lint`, `pnpm run check:boundaries`, and `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. **Actually launched and interacted with** via the established in-memory-platform-plus-headless-Chromium method: built a rule in the live Behaviour Mode UI on the FDraft Default Event template, switched its condition to a numeric progress range and confirmed the readable sentence/trace updated correctly; then built a real hover-driven rule (hovering the profile badge hides the event countdown) and **visually confirmed the countdown actually disappears on a genuine `mouseEnter` and reappears on mouse-out**, zero console errors throughout. `../FDraft` confirmed untouched via `git status` both before and after this phase. **Deferred, honestly incomplete:** a dedicated per-layer Interaction States editor in the Properties panel (hover/focus/pressed/disabled/entrance/exit authored directly on one layer) — the schema field (`layer.interactionStates`), the evaluator, and the real DOM event wiring all exist and are tested, but Studio's UI only reaches this capability indirectly through project-wide Behaviour rules, which are strictly more general for nearly every real case but are a different authoring surface than "select a layer, add a hover state" would be; `startAnimation`/`stopAnimation`/`restartAnimation` are fully validated and resolved by the evaluator (correct winning rule, real trace) but have **no visual effect yet**, since actual animation playback remains Prompt 9's reserved scope (the pre-existing `EffectLayerView` gap, unchanged); live click-driven popup-open/close and page-navigation dispatch inside Studio's own preview — `resolveTriggeredRule` correctly determines the winning rule for a click/page-lifecycle edge event and is unit-tested, but Studio hasn't wired a live click-through dispatch loop that performs the resulting on-screen navigation/popup open (continuous `whileTrue` rules, including real hover, **are** fully live in the simulator); drag-based rule priority reordering (up/down buttons instead, functionally equivalent); removing a rule's last action is silently prevented rather than disabled in the UI (the schema requires at least one). |
| 9. Animations and effects | FDraft-Studio | Implemented, deliberately scoped core | Filled in the two renderer areas Prompt 3 explicitly reserved (no animation playback, effect layers as placeholder shells only) with one shared, deterministic implementation Studio and FDraft both run — never a Studio-only interpretation — plus real Studio authoring and a performance inspector. See "Deferred" below for the honest gap list. **Schema (`@fdraft/theme-sdk`, all additive):** `AnimationDeclaration` gained `motion` (`{type:"preset", preset}` — 10 closed presets: `fade`, `rise`, `fall`, `slideLeft`, `slideRight`, `scalePop`, `float`, `wobble`, `pulse`, `sway` — or `{type:"keyframes", keyframes}`, up to 12 hand-authored steps of opacity/x/y/rotationDeg/scale), `repeat` (`once`/`count`≤100/`infinite` — "unbounded" must be the explicit, visible `infinite` choice, never a very large number standing in for it), `direction` (normal/reverse/alternate), `intensity` (0-2, scales a preset's amplitude), and `randomOffsetMs` (≤2000ms, seeded from the animation+layer id — organic-feeling without ever being irreproducible). The legacy single-property `property`/`from`/`to`/`loop` fields are kept exactly as before (a `.refine` requires one shape or the other) so no migration was needed. `AnimationTrigger` gained `manual` — never self-triggers; only a Behaviour rule's `startAnimation` says when it plays, which is how "on hover/focus/pressed" and a rule-gated idle loop are built, reusing Prompt 8's evaluator with zero new dispatch plumbing (`onStateChange`/`onInterval`/`onEventPhase` — pre-existing, parameter-less, and never functional — are treated identically to `manual`, documented rather than silently repurposed). `EffectKind` is now the exact 11-kind closed set the prompt asked for (`rain`/`snow`/`fog`/`leaves`/`dust`/`stars`/`embers`/`confetti`/`fireflies`/`filmGrain`/`clouds`, replacing the old placeholder `snow`/`confetti`/`particles`/`glow`). `EffectDeclaration` gained `speed`, `directionDeg` (0-360, documented convention), `sizeRange` (bounded, min≤max), `opacity`, `colorTokenId`, and `seed` — `intensity` remains the *only* density control (0-1); it is never a raw particle count, so a theme cannot request an unbounded number even in principle (see caps below). **Validation:** no new checks were needed — `checkBehaviourRules`'s existing `animationId` broken-reference check already covers `startAnimation`/`stopAnimation`/`restartAnimation`, and Prompt 7's `DECORATIVE_LAYER_COVERS_COMPONENT` design warning already treats effect layers as "decorative," so a foreground effect crossing a protected component was already caught — visually reconfirmed this phase (a full-bleed snow layer correctly produced warnings against every component beneath it). **The shared evaluator (`@fdraft/theme-renderer`):** `PERFORMANCE_TIER_CAPS` — documented, fixed caps per tier (`low`: 0 particles, 0 effect layers, animations and effects both off, everything renders in its resting/final state; `medium`: 40 particles/effect, 2 simultaneous effect layers, 1.5× device-pixel-ratio ceiling; `high`: 150 particles/effect, 4 layers, 2× DPR) — `resolveParticleCount` is the *only* place `intensity` becomes an actual number, always tier-capped. A seeded PRNG (`createSeededRandom`, mulberry32 — never `Math.random()`) drives both particle-field initialisation and animation delay jitter, so the same seed always reproduces the same field/timing, verified by exact-value tests. Real CSS-keyframe animation via `useLayerAnimation`: `onEnter` plays automatically from mount (`repeat: infinite` is how a continuously-looping idle motion is built, no rule needed); `onExit` keeps the layer mounted and playing for its full duration *before* actually hiding it (a genuinely new capability — previously `visible: false` hid a layer instantly via `display: none`, which can never be observed mid-transition); `manual` plays only while `BehaviourResolution.animationActiveOverrides[animationId]` says so, newly added to the same continuous-target conflict resolution `show`/`hide`/etc. already used. One shared canvas particle engine (`particleEngine.ts` + `EffectCanvas.tsx`) serves all 10 particle-based kinds through one config table (motion pattern — fall/rise/drift/twinkle/wander — shape, colour, speed, sway, rotation, opacity-pulse per kind) — never per-kind bespoke code, and never hundreds of editor-created image layers; `filmGrain` (not particle-shaped at all) renders through a separate, cheaper SVG `feTurbulence` path instead of being forced through the particle model. `usePlaybackGate` pauses the animation frame loop when the document is hidden, the window loses focus, or the canvas scrolls offscreen (`IntersectionObserver`) — real, tested pausing, not a suggestion. Resize/high-DPI: a `ResizeObserver` backs the canvas at `devicePixelRatio` capped by the active tier. Effect-layer-count protection is structural: `ThemeRenderer` computes the allowed set (first `maxEffectLayers` in z-order) once per render; anything beyond the cap renders nothing at all, not a degraded version. No blur/shadow filters are used anywhere in the particle engine — a deliberate, documented cost-avoidance decision (soft-looking kinds like fog/snow use low opacity and larger flat shapes instead). 42 new/changed tests in this package (124→166). **Studio:** `LayersPanel` gained "+ Effect…" (Studio had *no* generic "add a new layer" affordance at all before this phase — a real, pre-existing gap discovered while scoping this prompt; deliberately not backfilled for every layer type, only effects, which is what this prompt needed) creating a full-canvas effect layer above existing siblings. `PropertiesPanel` gained `EffectLayerFields` (intensity/speed/direction/size-range/opacity/seed) and a per-layer **Animations** section (trigger/preset/duration/delay/easing/repeat/direction/intensity/random-offset, add/delete) via new `effectCommands.ts`/`animationCommands.ts`. A new **Performance Inspector** (`PerformanceInspectorPanel.tsx`, opened from a new "Performance" TopBar button) reports total/animated-layer counts, each effect layer's real tier-capped approximate particle count, large-asset count (≥2MB), and the shared design-warning feed — worded throughout as approximate counts, explicitly never a measured frame rate or hardware benchmark. Reduced-motion and performance-tier are now real, stateful controls in the main status bar (previously `hostSettings` was a hardcoded constant reachable only inside Behaviour Mode's own separate simulator) — "a clear per-project preview" now applies to the actual page content in Design/Preview mode, not just Behaviour Mode's simulated one. 20 new studio tests (283→303). **Three real bugs caught and fixed by this phase's own tests, not deferred:** (1) moving master-inheritance resolution out of `PageStage` and into `ThemeRenderer` (needed so the effect-layer cap could be computed once, before rendering) initially broke the circular-master-chain fallback, since the resolution call ended up outside `RenderErrorBoundary`'s actual React descendant tree — caught immediately by the pre-existing `ThemeRenderer.test.tsx` regression suite, fixed by introducing a `RenderedStage` descendant component mirroring the established `ThrowRenderError` pattern; (2) a `document` variable-shadowing bug in `PageStage.tsx` — the `RenderableDocument` context value is also (necessarily) named `document`, and a leftover `stageStyle(document.canvas)` call silently started reading the browser's global `Document` instead once the surrounding code was refactored — caught by inspection before it ever reached a test; (3) `usePlaybackGate`'s initial focus state relied on `document.hasFocus()`, which defaults to `false` in the very environment used to test it (and is unreliable across some embedding contexts generally) — caught by a dedicated test asserting the gate starts active, fixed by defaulting to optimistically focused and reacting only to real `blur`/`focus` events. Full workspace: `pnpm -r typecheck`, `pnpm -r test` (613 tests: theme-sdk 144, theme-renderer 166, studio 303), `pnpm run lint`, `pnpm run check:boundaries`, and `pnpm --filter @fdraft/theme-sdk check:schemas` all pass clean. Both `@fdraft/theme-sdk` and `@fdraft/theme-renderer` rebuilt and `pnpm pack`-verified (the packed `theme-renderer` tarball's `package.json` correctly pins `"@fdraft/theme-sdk": "0.1.0"`, not `workspace:*`) — ready for the repository owner to cut real releases whenever they choose; version numbers were not bumped unilaterally, the same standing deferral as every prior phase. **Actually launched and visually verified** via the established in-memory-platform-plus-headless-Chromium method: added snow, confetti, and film-grain effect layers simultaneously to the FDraft Default Event template and watched real particle motion between two screenshots taken 800ms apart (a snowflake visibly fell and drifted); confirmed confetti renders as rotating pink rectangles and film grain as an animated noise texture, exactly matching each kind's configured shape/colour; added a `pulse`/`infinite` animation to a text layer; switched the performance tier to Low in the status bar and confirmed every effect vanished completely (canvas elements removed from the DOM, not just paused); opened the Performance Inspector and confirmed it reported 32 layers, 1 animated layer, 3/4 effect layers allowed at High with real per-effect particle-count estimates, and — most importantly — 18 real warnings correctly flagging the newly-added full-bleed snow layer for sitting above every protected component beneath it. Zero console errors throughout. `../FDraft` confirmed untouched via `git status` both before and after this phase. **Deferred, honestly incomplete:** Studio has no UI for hand-authoring a *custom* keyframe list (the schema, evaluator, and CSS generation all support up to 12 keyframes and are tested directly, but `PropertiesPanel`'s Animations section only offers the 10 presets — a real, meaningful gap, not a rounding error); no visual/drag-based keyframe timeline editor was attempted even for that reason; live click-driven `restartAnimation`/popup/navigation dispatch inside Studio's own preview remains unwired, the same standing deferral Prompt 8 already documented, now also covering animations specifically (continuous `whileTrue`-driven animations, including real hover, are fully live); no automated wall-clock performance-timing budget assertions were added (deliberately — timing-based test assertions are notoriously flaky on shared CI runners) — instead the *structural* caps (particle-count ceilings, effect-layer ceilings, DPR ceilings) are the enforced budget, verified with deterministic unit tests rather than timing measurements; high-DPI handling reads and caps `devicePixelRatio` and is unit-tested, but was not verified against an actual physical high-DPI display, only a headless Chromium run at DPR 1; audio was deliberately not touched anywhere, per the prompt's own explicit instruction. |
| 10. FDraft runtime integration | FDraft | Not started | |
| 11. Simulator, live preview, and publishing | FDraft-Studio | Not started | |
| 12. Cross-repository hardening and installers | Both | Not started | |
| 13. Official event starter projects | Both | Not started | |

## Current blockers

- **Action needed from the repository owner, more urgent now that Prompt 10 is next:** cut
  the first releases of `@fdraft/theme-sdk` and `@fdraft/theme-renderer` by bumping each
  package's version if needed, committing, and pushing a `theme-sdk-v<version>` /
  `theme-renderer-v<version>` tag. Both packages are built and `pnpm pack`-verified as of
  Prompt 9 (the packed `theme-renderer` tarball's `package.json` correctly pins
  `"@fdraft/theme-sdk": "0.1.0"`, not `workspace:*`) — genuinely ready to release, version
  numbers just weren't bumped unilaterally. `.github/workflows/release-theme-sdk.yml` /
  `release-theme-renderer.yml` handle the rest (gate, pack, checksum, GitHub Release).
  Prompt 10 (FDraft runtime integration) is the first phase that will actually need FDraft
  to consume a real, exact-pinned version of both packages rather than the in-repo pnpm
  workspace link, so this is worth doing before that phase starts.
- Known-good FDraft base: `main` (`v1.2.0-beta.9`) is the candidate; needs explicit user
  confirmation before Prompt 10. The sibling checkout is currently sitting on
  `feat/dev-build1-event-studio` (the failed Event Studio branch), 9 commits ahead of
  `main` — not treated as a base candidate.
- Real FDraft component adapters are unimplemented by design until Prompt 10 — see
  `docs/architecture/RENDERER_HOST_NOTES.md` for read-only candidate mappings.
- The renderer's reserved gaps (animation/effect playback, font loading, crop zoom,
  device-pixel-constant anchors, nested relative coordinates) are listed in
  `packages/theme-renderer/README.md` and expected to close in later prompts (9, 5-7).
- `apps/studio/src-tauri/Cargo.lock` is generated but not yet committed (only committed
  when the user asks for a commit).
- Prompt 5's Copy Workspace (project-wide grouped/searchable copy editing, placeholder
  insertion, reset/duplicate/where-used/safe-replace, overflow/contrast/unresolved-
  placeholder warnings), full smart-guide rendering, rich-text model, asset-based/rounded
  masks, and layer accessibility/description fields are all deferred — see the Phase 5 row
  above for the specific reasons each was scoped out rather than silently dropped.
- Guides and safe areas have no home in `@fdraft/theme-sdk`'s schema yet — persisting
  user-created guides or configuring visible safe areas needs a small schema addition,
  which was judged too significant to make unilaterally inside Prompt 5's scope; needs an
  explicit decision before that work can start.
- Prompt 6's native OS drag-and-drop import and clipboard-paste import are implemented but
  **not verified against the real Tauri desktop app** — only against the browser dev
  harness, consistent with this project's established screen-capture-safety policy (see
  memory: never attempt a blind/ambiguous-target OS screenshot). `dragDropEnabled: false`
  is now set in `tauri.conf.json`; the next person to run `pnpm tauri dev` should drag a
  real file onto the Asset Workspace and confirm it imports before this is treated as
  fully proven.
- Non-destructive image optimisation (`apps/studio/src/assets/imageOps.ts`) has no UI —
  the re-encode function exists and composes with `buildReplaceAssetSourceCommand`, but
  nobody can reach it from the Asset Workspace yet.
- Prompt 7's responsive-design bullets are the largest deliberate scope-down of the phase:
  no true nine-point anchor *stretch*, fixed/fill/hug sizing, 4-sided margins, min/max
  width/height constraints, proportional scaling across breakpoints, a preview-profile
  switcher (Desktop/Laptop/Small Window/Custom), or a responsive inspection mode exist yet —
  only single-edge/center anchoring and one-active-breakpoint transform overrides
  (pre-dating this phase) are implemented. See the Phase 7 row above for the full list.
- `ZoneKind` (header/sidebar/main/overlay/footer) is a logical classification tag on
  `ComponentLayer` only — there is no visual, resizable zone-region editor yet. Themes
  still can't be checked for "did this land in a compatible zone" except via the
  Validation panel's `INCOMPATIBLE_ZONE` warning.
- Border/gradient/shadow token picker UI in the Properties panel is deferred — the schema
  fields (`fillGradientTokenId`, `shadowTokenIds`, border tokens) and renderer wiring exist
  and are tested, and `apps/studio/src/editor/tokenCommands.ts` can create all five token
  types, but only Fill-color and Corner-radius got quick-create UI wired into
  `PropertiesPanel.tsx` this phase (border/gradient/shadow each need an existing
  `colorTokenId` dependency and a good compound quick-create UX was judged out of budget).
- Prompt 8's Behaviour Mode has no dedicated per-layer Interaction States editor in the
  Properties panel — `layer.interactionStates`, the evaluator, and the real hover/focus/
  pressed/selected DOM wiring all exist and are tested, but Studio's UI only reaches this
  through project-wide Behaviour rules (`BehaviourWorkspace`), a different, more general
  authoring surface than "select one layer, add a hover state" would be.
- `startAnimation`/`stopAnimation` Behaviour actions (`whileTrue` rules) now have a full
  visual effect as of Prompt 9 — `restartAnimation` and every other imperative action still
  only resolve as data pending the live click-dispatch work below.
- Live click-driven popup-open/close, page-navigation, and `restartAnimation` dispatch
  inside Studio's own preview is not wired — `resolveTriggeredRule` correctly determines the
  winning rule for a click/page-lifecycle edge event and is unit-tested, but nothing in
  Studio's preview calls it on a real click and performs the resulting on-screen navigation/
  popup-open/animation-restart yet. Continuous (`whileTrue`) rules, including real hover/
  focus/pressed/selected and now animation start/stop, **are** fully live in both the
  Behaviour Mode simulator and Design/Preview mode today.
- Prompt 9's Studio Animations section (Properties panel) only offers the 10 built-in
  presets — the schema, evaluator, and CSS-keyframe generation all fully support up to 12
  hand-authored custom keyframes (opacity/x/y/rotation/scale per step) and are tested
  directly against the renderer, but no UI exists yet to author a custom keyframe list, let
  alone a visual/drag-based timeline editor.
- No automated wall-clock performance-timing budget assertions exist for animations/effects
  (deliberately — timing-based test assertions are flaky on shared CI runners). The actual
  enforced budget is structural: fixed, documented per-tier particle/effect-layer/DPR caps
  (`PERFORMANCE_TIER_CAPS`), verified with deterministic unit tests instead.
- High-DPI canvas scaling reads and caps `devicePixelRatio` and is unit-tested, but has not
  been verified against a real physical high-DPI display — only a headless Chromium run at
  device-pixel-ratio 1, consistent with this project's established screenshot-safety
  caution around anything not literally checked on real hardware.

