# `@fdraft/theme-renderer`

Prompt 3 builds the shared renderer here. Studio and FDraft consume the same released implementation with different injected component adapters and render contexts.

It must not import the Studio application or FDraft business logic.

