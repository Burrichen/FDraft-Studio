# `@fdraft/theme-sdk`

Prompt 2 builds the framework-neutral theme contract here: schemas, validation, migrations, packaging, deterministic compilation, compatibility checks, security limits, and CLI tools.

It must not depend on React, Tauri, the DOM, Studio UI code, or FDraft business logic.

