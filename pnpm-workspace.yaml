packages:
  - apps/*
  - packages/*
allowBuilds:
  # esbuild's postinstall only downloads its platform binary; required for
  # vitest/tsx (which depend on it) to run at all.
  esbuild: true
minimumReleaseAgeExclude:
  - '@testing-library/user-event@14.6.7'
  - globals@17.12.0
