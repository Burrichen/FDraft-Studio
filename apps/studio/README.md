# Studio application

Prompt 4 creates the standalone Tauri + React + TypeScript application here after the SDK and renderer have passed their own tests. Do not begin by generating a decorative editor shell.

